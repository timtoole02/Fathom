# Public launch evidence snapshot

This snapshot records the current public launch verification state for Fathom. It is evidence for the narrow local public contract only; it is not production-readiness, performance, quality, legal/license, full OpenAI parity, streaming, external proxying, or broad model-runtime evidence.

## Snapshot

- Launch baseline commit: `a32505eadac6539865d224a8b4195656003a0032` (`Close out public launch readiness phase`)
- Latest no-download refusal evidence commit: `1b930d4ed2411fe72e66d45acc757318b649d730` (`Prove embeddings malformed JSON refusal`)
- Latest optional API evidence commit: `0655fc073af296f7687a04708053a30b9570fcdf` (`Record MiniLM embeddings API acceptance evidence`)
- Latest optional artifact QA CI wiring commit: `e9195bc7462999284960f5631d3a74aa5391bffc` (`Wire optional artifact QA into CI`)
- Latest public-contract QA hardening commit: `1fe6eef8ad99378318f7489dfdbbb66ee8716761` (`Guard CI secret access`)
- Latest public risk-scan hardening commit: `e2c8c929e63302d9ad5b2eaee3330cd93e491705` (`Guard CI runner temp artifacts`)
- Scope: no-download public `/v1` contract, public launch checklist/evidence snapshot, manifest-driven smoke, sanitized public-contract smoke artifacts, public-contract smoke duplicate-entry schema guards, public-contract smoke Markdown/status/proof-scope row consistency, failed public-contract smoke endpoint/boundary drift guards, malformed `/v1` JSON request body refusal smoke, unsupported `/v1` route/method public-contract refusal smoke, synthetic PyTorch `.bin` public-contract refusal smoke, unsupported ONNX chat/general public-contract refusal smoke, unverified SafeTensors/Hugging Face public-contract refusal smoke, metadata-only GGUF public-contract refusal smoke, offline public-contract and backend acceptance artifact QA, backend acceptance artifact summary share-safety guard, manifest shape validation, manifest-to-`/v1` docs boundary coverage, manifest `base_url` alignment across public docs/examples, public refusal boundary status/code/request-hint coverage, local API no-auth/loopback security-note coverage, repository-level MIT license metadata/docs consistency guard, repository text-normalization metadata guard for binary model/package/mobile/media/local-plan artifacts, API example loopback default and stdout share-safety guards, REST Client `.http` request-order/header guards and negative self-tests, tracked Python and shell syntax coverage guards, lockfile-reproducible frontend clean install guard, README clean-install consistency guard, frontend package script loopback/private-publish guard, Rust crate publish-safety guard, frontend build/UI copy launch-gate guards in the checklist and CI, CI whitespace-check wiring guard, CI checkout credential persistence guard, CI privileged PR trigger, Node cache-scope, bounded job-timeout, concurrency-cancellation, and secret-access guards, CONTRIBUTING and PR template syntax-gate consistency checks, public security/privacy reporting copy guard, tracked-file secret-token/private-key/cloud API-key and macOS/Linux/Windows home/profile path guards, dependency lockfile local/private source guard, Git submodule metadata local/private source guard, Git LFS pointer-file guard, tracked cloud SDK credential/config guard, root and nested `.gitignore` local cloud SDK credential/config guard, tracked Kubernetes credential/config guard, root and nested `.gitignore` local Kubernetes credential/config guard, OS/platform metadata-file, root `.gitignore` local OS/platform metadata-file guard, editor backup/swap artifact guard, local editor history artifact guard, patch/diff artifact guard, root `.gitignore` local editor backup/swap artifact guard, root `.gitignore` local editor history artifact guard, root `.gitignore` local patch/diff artifact guard, IDE workspace/config artifact guard including `.zed/`, root and nested `.gitignore` local IDE workspace/config artifact guard including `.zed/`, credential/config filename guard including SSH private-key filenames, `.ssh/` directories, direnv config/state, Git credential/config files, generic secret material paths, and TLS certificate/request artifacts, root and nested `.gitignore` local credential/config guard including SSH private-key filenames, `.ssh/`, direnv config/state, Git credential/config files, generic secret material patterns, and TLS certificate/request artifact patterns, workspace/personal agent context-file guard including local assistant state such as `.cursor/` and `.windsurf/`, root and nested `.gitignore` personal workspace context guard including local assistant state, local runtime/artifact detail-file guard including DuckDB databases/WAL files and Redis RDB snapshots, root and nested `.gitignore` local runtime/artifact detail-file guard including DuckDB databases/WAL files and Redis RDB snapshots, local shell/REPL command history-file guard, root and nested `.gitignore` local shell/REPL command history-file guard, local log/trace/profiling/debug-output artifact guard including SARIF/code-scanning reports and crash dumps, root `.gitignore` local log/trace/profiling/debug-output artifact guard including SARIF/code-scanning reports and crash dumps, Python cache/build artifact guard including Hypothesis example databases, root and nested `.gitignore` local Python cache/build artifact guard, Python virtualenv/dependency artifact guard, root and nested `.gitignore` local Python virtualenv/dependency artifact guard, Ruby/Bundler dependency artifact guard, root and nested `.gitignore` local Ruby/Bundler dependency artifact guard, PHP Composer dependency/test artifact guard, root and nested `.gitignore` local PHP Composer dependency/test artifact guard, JVM compiler bytecode artifact guard, root and nested `.gitignore` local JVM compiler artifact guard, .NET/NuGet local dependency/build/user-state artifact guard, root `.gitignore` local .NET/NuGet artifact guard, Clojure/Leiningen local artifact guard, root and nested `.gitignore` local Clojure/Leiningen artifact guard, Kotlin/Kotlin Native compiler artifact guard, root and nested `.gitignore` local Kotlin/Kotlin Native artifact guard, Scala/SBT local build server/IDE artifact guard, root and nested `.gitignore` local Scala/SBT build artifact guard, local Nix build result artifact guard, root `.gitignore` local Nix build result artifact guard, Dart/Flutter local tool/cache artifact guard, root and nested `.gitignore` local Dart/Flutter artifact guard, Bazel local build output guard, root `.gitignore` local Bazel build artifact guard, Buck/Buck2 local build output guard, root and nested `.gitignore` local Buck/Buck2 build artifact guard, frontend/Node cache/build artifact guard including package-manager caches/stores, Deno local caches, Vite/Vitest cache/config timestamp artifacts, Storybook static build output, and lint cache files, root and nested `.gitignore` local frontend/Node cache/build artifact guard including package-manager caches/stores, Deno local caches, Vite/Vitest cache/config timestamp artifacts, Storybook static build output, and lint cache files, ESLint report artifact guard, root and nested `.gitignore` local ESLint report artifact guard, local cache artifact guard including Watchman local state cookies, root and nested `.gitignore` local cache artifact guard including Watchman local state cookies, local temporary/scratch artifact guard, root and nested `.gitignore` local temporary/scratch artifact guard, local test report/coverage artifact guard including Playwright/browser-test reports, Python coverage data files, and LCOV outputs, root and nested `.gitignore` local test report/coverage artifact guard including Playwright/browser-test reports, Python coverage data files, and LCOV outputs, local notebook checkpoint artifact guard, local notebook runtime/config artifact guard, tracked notebook execution-output guard, root and nested `.gitignore` local notebook artifact guard, Rust/Cargo cache/build artifact guard, root and nested `.gitignore` local Rust/Cargo cache/build artifact guard, Go cache/test artifact guard, root and nested `.gitignore` local Go cache/test artifact guard, Elixir/Mix build/dependency artifact guard, root and nested `.gitignore` local Elixir/Mix build/dependency artifact guard, Erlang/Rebar3 cache/crash artifact guard, root and nested `.gitignore` local Erlang/Rebar3 artifact guard, Haskell Stack/Cabal build artifact guard, root and nested `.gitignore` local Haskell Stack/Cabal build artifact guard, OCaml/opam local switch artifact guard, root and nested `.gitignore` local OCaml/opam artifact guard, Lua/LuaRocks dependency/build/package artifact guard, root and nested `.gitignore` local Lua/LuaRocks artifact guard, Zig build artifact guard, root and nested `.gitignore` local Zig build artifact guard, release/package artifact guard for common archive, JVM package, and installer extensions, root `.gitignore` local release/package artifact guard, backup/dump artifact guard, root `.gitignore` local backup/dump artifact guard, local model/checkpoint artifact guard, root `.gitignore` local model/checkpoint artifact guard, local ML experiment/tracking artifact guard, root and nested `.gitignore` local ML experiment/tracking artifact guard, local Docker/container artifact guard, root `.gitignore` local Docker/container artifact guard, local deployment platform artifact guard, root `.gitignore` local deployment platform artifact guard, local Terraform/OpenTofu/Terragrunt infrastructure state artifact guard, root `.gitignore` local infrastructure state guard, local mobile/Xcode/Android build artifact guard, root `.gitignore` local mobile/Xcode/Android build artifact guard, local screenshot/screen-recording artifact guard, root `.gitignore` local screenshot/screen-recording artifact guard, local audio/video capture/export artifact guard, root `.gitignore` local audio/video capture/export artifact guard, and symlink escape and target-resolution guards, public risk-scan caveat coverage that preserves that the scanner is not a complete privacy audit, OpenAI SDK example offline request/response regression, API contract issue-template privacy checks, required public issue-template field/privacy checkbox guard, blank public issue disablement checks, bug/security issue-template privacy checks, PR template truthfulness/privacy checks, CI token-permissions guard, CI public risk-scan wiring guard, public overclaim scanner self-test coverage, production-readiness/legal-license public overclaim examples, offline MiniLM/SmolLM2/Qwen2.5 optional API acceptance artifact QA self-tests, CI/static policy wiring, and Phase 16 narrow catalog-backed optional API/runtime evidence for MiniLM embeddings, SmolLM2 135M Instruct, and Qwen2.5 0.5B Instruct.
- Additional current public risk-scan scope: R/RStudio local session/dependency artifact guard and matching root `.gitignore` local R/RStudio artifact coverage.
- Additional current public risk-scan scope: Git credential/config guard for `.git-credentials`, `.gitconfig`, and `.gitconfig.local`, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: IntelliJ project file guard for `*.ipr` and `*.iws`, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: Stylelint report artifact guard for `stylelint-report/`, `stylelint-reports/`, `stylelint-junit.xml`, `stylelint-report.json`, `stylelint-report.xml`, `stylelint-results.json`, and timestamped `stylelint-report-*` or `stylelint-results-*` files, with matching root and nested `.gitignore` coverage while preserving source-of-truth Stylelint configuration such as `stylelint.config.*` and `.stylelintrc*`.
- Additional current public risk-scan scope: Biome report artifact guard for `biome-report/`, `biome-reports/`, `biome-report.html`, `biome-report.json`, `biome-report.xml`, `biome-results.json`, and timestamped `biome-report-*` or `biome-results-*` files, with matching root and nested `.gitignore` coverage while preserving source-of-truth Biome configuration such as `biome.json` and `biome.jsonc`.
- Additional current public risk-scan scope: Markdown/prose lint report artifact guard for `markdownlint-report/`, `markdownlint-reports/`, `markdownlint-junit.xml`, `markdownlint-report.json`, `markdownlint-results.xml`, `remark-report/`, `remark-reports/`, `remark-junit.xml`, `remark-report.json`, `remark-results.xml`, `proselint-report/`, `proselint-reports/`, `proselint-junit.xml`, `proselint-report.json`, and `proselint-results.xml`, with matching root and nested `.gitignore` coverage while preserving source-of-truth Markdown/prose lint configuration such as `.markdownlint.*`, `.remarkrc`, `remark.config.*`, and `.proselintrc`.
- Additional current public risk-scan scope: TypeScript type-check report artifact guard for `tsc-report/`, `tsc-report.txt`, `typecheck-report/`, `typecheck-results.json`, `typescript-report.xml`, and timestamped `tsc-report-*`, `typecheck-results-*`, or `typescript-results-*` files, with matching root and nested `.gitignore` coverage while preserving source-of-truth TypeScript project configuration such as `tsconfig.json`, `tsconfig.*.json`, frontend package manifests, and JavaScript/TypeScript source files.
- Additional current public risk-scan scope: Julia depot/preference artifact guard for `.julia/`, `LocalPreferences.toml`, `*.jl.cov`, and `*.jl.mem`, with matching root and nested `.gitignore` coverage and source-file allowances preserving source-of-truth Julia project files such as `Project.toml`, `Manifest.toml`, `Artifacts.toml`, and Julia source files.
- Additional current public risk-scan scope: Gradle/JVM local build artifact guard for `.gradle/` cache/state at any tree depth and common Gradle `build/` output subtrees, with matching root and nested `.gitignore` coverage for Gradle cache/state and project-file allowances for `build.gradle`, `settings.gradle`, `gradle.properties`, `gradlew`, and `gradle/wrapper/gradle-wrapper.properties`.
- Additional current public risk-scan scope: PHP Composer local dependency/test artifact guard for Composer vendor/PHPUnit cache artifacts at any tree depth, with matching root and nested `.gitignore` coverage and source-file allowances for Composer manifests, PHP source files, and PHP docs.
- Additional current public risk-scan scope: Bazel local build output guard for `bazel-bin/`, `bazel-out/`, `bazel-testlogs/`, and root `bazel-*` outputs, with matching root `.gitignore` coverage and source-file allowances for `BUILD`, `BUILD.bazel`, `MODULE.bazel`, and `.bzl` files.
- Additional current public risk-scan scope: Buck/Buck2 local build output guard for `.buckd/` and `buck-out/`, with matching root and nested `.gitignore` coverage and source-file allowances for `BUCK`, `BUCK.v2`, and `.buckconfig`.
- Additional current public risk-scan scope: ML experiment/tracking artifact guard for W&B, MLflow, Lightning, and TensorBoard run outputs, with matching root and nested `.gitignore` coverage.
- Additional current public risk-scan scope: Elixir/Mix local build/dependency artifact guard for `.elixir_ls/`, `_build/`, and `deps/` at any tree depth, with matching root and nested `.gitignore` coverage and source-file allowances for `mix.exs`, `mix.lock`, Elixir source files, and Elixir docs.
- Additional current public risk-scan scope: Dart/Flutter local tool/cache artifact guard for `.dart_tool/`, `.pub-cache/`, `.pub/`, `.packages`, `.flutter-plugins`, and `.flutter-plugins-dependencies` at any tree depth, with matching root and nested `.gitignore` coverage and source-file allowances for `pubspec.yaml`, `pubspec.lock`, Dart source, Android Gradle project files, and iOS Xcode project files.
- Additional current public risk-scan scope: Expo local project state guard for `.expo/` and `.expo-shared/` at any tree depth, with matching root and nested `.gitignore` coverage and source-file allowances for mobile app source, Expo config, Android Gradle project files, and iOS Xcode project files.
- Additional current public risk-scan scope: Android native build intermediate guard for `.cxx/` and `.externalNativeBuild/` at any tree depth, including the same directories under `android/`, with matching root, nested, and `android/` `.gitignore` coverage and source-file allowances for Android Gradle project files, JNI/C++ source files, and checked-in native build configuration.
- Additional current public risk-scan scope: CocoaPods dependency output guard for root `Pods/` and `ios/Pods/`, with matching root `.gitignore` coverage and source-file allowances for `Podfile` and `Podfile.lock`.
- Additional current public risk-scan scope: Carthage dependency output guard for `Carthage/Build/` and `Carthage/Checkouts/`, with matching root `.gitignore` coverage and source-file allowances for `Cartfile` and `Cartfile.resolved`.
- Additional current public risk-scan scope: Fastlane generated report/test artifact guard for `fastlane/report.xml`, `fastlane/Preview.html`, and `fastlane/test_output/`, with matching root `.gitignore` coverage and source-file allowances for Fastlane configuration and App Store metadata such as `Fastfile`, `Appfile`, and `fastlane/metadata/`.
- Additional current public risk-scan scope: general mobile/Xcode/Android build artifact guard for root, nested, Android-specific, and platform artifact `.gitignore` coverage of local mobile build outputs.
- Additional current public risk-scan scope: PDM/PEP 582 local Python dependency/build artifact guard for `.pdm-build/` and `__pypackages__/`, plus virtualenv and package-manager cache directories at any tree depth, with matching root and nested `.gitignore` coverage.
- Additional current public risk-scan scope: Nix build result artifact guard for root `result` and `result-*` outputs, with matching root `.gitignore` coverage and source-file allowances for `flake.nix`, `flake.lock`, `default.nix`, `shell.nix`, and `*.nix` files.
- Additional current public risk-scan scope: local Kotlin/Kotlin Native compiler artifact guard for `.kotlin/` and `.konan/` at any tree depth, with matching root and nested `.gitignore` coverage and source-file allowances for `build.gradle.kts`, `settings.gradle.kts`, Kotlin source files, and Gradle version catalog files.
- Additional current public risk-scan scope: Clojure/Leiningen local artifact guard for `.lein/`, `.cpcache/`, and `.shadow-cljs/` at any tree depth, plus `.nrepl-port`, with matching root and nested `.gitignore` coverage and source-file allowances for `project.clj`, `deps.edn`, `bb.edn`, `shadow-cljs.edn`, and Clojure/ClojureScript source files.
- Additional current public risk-scan scope: Scala/SBT local build server/IDE artifact guard for `.bloop/`, `.bsp/`, `.metals/`, and `.scala-build/` at any tree depth, with matching root and nested `.gitignore` coverage and source-file allowances for `build.sbt`, `project/build.properties`, and Scala source files.
- Additional current public risk-scan scope: Haskell Stack/Cabal local build artifact guard for `.stack-work/`, `dist-newstyle/`, `.cabal-sandbox/`, and `cabal.sandbox.config` at any tree depth, with matching root and nested `.gitignore` coverage and source-file allowances for `.cabal` files, `stack.yaml`, `cabal.project`, and Haskell source files.
- Additional current public risk-scan scope: Zig local build artifact guard for `.zig-cache/`, `zig-cache/`, and `zig-out/` at any tree depth, with matching root and nested `.gitignore` coverage and source-file allowances for `build.zig`, `build.zig.zon`, and Zig source files.
- Additional current public risk-scan scope: local OCaml/opam artifact guard for `_opam/` and `.opam-switch/` at any tree depth, with matching root and nested `.gitignore` coverage and source-file allowances for `dune`, `dune-project`, `*.opam`, and OCaml source files.
- Additional current public risk-scan scope: Autotools local configure/Libtool output guard for `autom4te.cache/`, `.deps/`, `.libs/`, `config.log`, `config.status`, generated `libtool` files, `*.lo`, and `*.la`, with matching root `.gitignore` coverage and source-file allowances for `configure.ac`, `Makefile.am`, and Autotools docs.
- Additional current public risk-scan scope: SCons local configuration/cache output guard for `.sconf_temp/`, `sconf_temp/`, `.sconsign`, and `.sconsign.dblite`, with matching root and nested `.gitignore` coverage and source-file allowances for `SConstruct` and `SConscript`.
- Additional current public risk-scan scope: Lua/LuaRocks local dependency/build/package artifact guard for `.luarocks/`, `lua_modules/`, and `*.rock` at any tree depth, with matching root and nested `.gitignore` coverage and source-file allowances for `*.lua`, `*.rockspec`, LuaRocks lock files, and Lua config files.
- Additional current public risk-scan scope: Perl/CPAN build/dependency artifact guard for `.cpan/`, `.cpanm/`, `blib/`, `local/lib/perl5/`, `MYMETA.json`, `MYMETA.yml`, and `pm_to_blib`, with matching root and nested `.gitignore` coverage and source-file allowances for `cpanfile`, `cpanfile.snapshot`, `Makefile.PL`, `Build.PL`, Perl source files, and Perl docs.
- Additional current public risk-scan scope: Erlang/Rebar3 local cache/crash artifact guard for `.rebar3/`, `rebar3.crashdump`, and `erl_crash.dump`, with matching root and nested `.gitignore` coverage and source-file allowances for `rebar.config`, `rebar.lock`, Erlang source files, and Erlang header files.
- Additional current public risk-scan scope: JVM compiler bytecode artifact guard for `*.class` and `*.tasty`, with matching root and nested `.gitignore` coverage and source-file allowances for JVM project files and Java/Kotlin/Scala source files.
- Additional current public risk-scan scope: .NET/NuGet local dependency/build/user-state artifact guard for `.nuget/`, `packages/`, `bin/Debug/`, `obj/Release/`, `project.assets.json`, `project.nuget.cache`, `*.nupkg`, `*.snupkg`, `*.csproj.user`, and `*.suo`, with matching root `.gitignore` coverage and source-file allowances for .NET project files, solution files, source files, and NuGet lock files.
- Additional current public risk-scan scope: Deno local cache artifact guard for `.deno/` and `deno-dir/`, with matching root and nested `.gitignore` coverage and source-file allowances for `deno.json`, `deno.jsonc`, `deno.lock`, JavaScript files, and TypeScript files.
- Additional current public risk-scan scope: frontend bundle-analysis output guard for `bundle-analyzer-report.html`, `bundle-analysis.json`, `bundle-stats.html`, `webpack-stats.json`, `rollup-stats.html`, `vite-bundle-visualizer.html`, and name-suffixed variants, with matching root and nested `.gitignore` coverage while preserving source-of-truth bundler configuration and package metadata.
- Additional current public risk-scan scope: SonarQube/SonarScanner local analysis outputs (`.scannerwork/`, `.sonar/`) as local code-quality scanner artifacts, with root and nested `.gitignore` coverage while preserving source-of-truth scanner configuration such as `sonar-project.properties`.
- Additional current public risk-scan scope: Semgrep local cache/state directories (`.semgrep/`) as local code-quality scanner artifacts, with root and nested `.gitignore` coverage while preserving source-of-truth Semgrep configuration such as `.semgrep.yml` and `semgrep.yml`.
- Additional current public risk-scan scope: Python static-analysis report outputs from Ruff, Pylint, Mypy, and Pyright (`ruff-report/`, `ruff-report.json`, `pylint-report.txt`, `mypy-report/`, `mypy-results.xml`, `pyright-report.json`, and timestamped `*-report-*` or `*-results-*` files) as local code-quality report artifacts, with matching root and nested `.gitignore` coverage while preserving source-of-truth Python analysis configuration such as `pyproject.toml`, `ruff.toml`, `.ruff.toml`, `.pylintrc`, `mypy.ini`, and `pyrightconfig.json`.
- Additional current public risk-scan scope: Lighthouse/LHCI audit outputs such as `.lighthouseci/`, `lhci_reports/`, `lighthouse-report.html`, `lighthouse-report.json`, and timestamped `lighthouse-report-*` files, with matching root, nested, and `frontend/` `.gitignore` coverage for generated Lighthouse/LHCI audit outputs while preserving source-of-truth Lighthouse config files such as `.lighthouserc.*`.
- Additional current public risk-scan scope: Axe/Pa11y accessibility audit report outputs such as `axe-report/`, `axe-reports/`, `axe-report.html`, `axe-results.json`, `pa11y-report/`, `pa11y-reports/`, `pa11y-ci-report.json`, and `pa11y-results.json`, with matching root and nested `.gitignore` coverage while preserving source-of-truth accessibility configuration such as `axe.config.*`, `.pa11yci`, and `pa11yci.json`.
- Additional current public risk-scan scope: security/dependency scan report artifact guard for generated Snyk, Cargo Audit, Cargo Deny, npm audit, pnpm audit, Yarn audit, OSV Scanner, Trivy, Grype, and OWASP Dependency-Check outputs such as `snyk-report/`, `cargo-audit-report/`, `cargo-deny-report/`, `npm-audit-report/`, `pnpm-audit-report/`, `yarn-audit-report/`, `osv-scanner-report/`, `trivy-report/`, `grype-report/`, `dependency-check-report/`, `snyk-report.json`, `cargo-audit-results.xml`, `cargo-deny-results.txt`, `npm-audit.json`, `osv-scanner-results.json`, `trivy-results.json`, `grype-results.txt`, and `dependency-check-report.html`, with matching root and nested `.gitignore` coverage while preserving source-of-truth lockfiles and scanner configuration such as `Cargo.lock`, `package-lock.json`, `.snyk`, `deny.toml`, `cargo-deny.toml`, `osv-scanner.toml`, `trivy.yaml`, `grype.yaml`, and `dependency-check.properties`.
- Additional current public risk-scan scope: SBOM/license inventory artifact guard for generated CycloneDX, SPDX, Syft, and license inventory outputs such as `sbom/`, `sboms/`, `cyclonedx/`, `cyclonedx-reports/`, `license-report/`, `license-reports/`, `licenses-report/`, `licenses-reports/`, `syft-report/`, `syft-reports/`, `sbom.json`, `sbom.spdx.json`, `bom.json`, `cyclonedx.json`, `syft-report.json`, and `license-report.json`, with matching root and nested `.gitignore` coverage while preserving source-of-truth SBOM or inventory tool configuration such as `cdxgen.yml`, `.cdxgenrc`, `syft.yaml`, and `.syft.yaml`.
- Additional current public risk-scan scope: Nx and SWC local frontend cache artifact guard for `.nx/` and `.swc/`, with matching root and nested `.gitignore` coverage.
- Additional current public risk-scan scope: Mochawesome report outputs (`mochawesome-report/`, `mochawesome.html`, `mochawesome.json`) as local test report artifacts, with matching root and nested `.gitignore` coverage.
- Additional current public risk-scan scope: Cucumber/BDD report outputs (`cucumber-report/`, `cucumber-reports/`, `cucumber-report.html`, `cucumber-report.ndjson`, `cucumber.json`, `cucumber-report.json`, `cucumber-report.xml`, `cucumber.xml`) as local test report artifacts, with matching root and nested `.gitignore` coverage where directory-shaped.
- Additional current public risk-scan scope: standalone xUnit XML report outputs (`xunit.xml`, `*.xunit.xml`) as local test report artifacts, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: standalone JUnit XML suite report outputs (`TEST-*.xml`) as local test report artifacts, with matching root `.gitignore` coverage for generated JUnit suite report files.
- Additional current public risk-scan scope: standalone JUnit XML aggregate suite report outputs (`TESTS-*.xml`) as local test report artifacts, with matching root `.gitignore` coverage for generated JUnit aggregate suite report files.
- Additional current public risk-scan scope: JUnit report directories and aggregate result files (`junit-report/`, `junit-reports/`, `junit-results.xml`) as local test report artifacts, with matching root and nested `.gitignore` coverage for JUnit report directories and root `.gitignore` coverage for generated JUnit result files.
- Additional current public risk-scan scope: TestNG generated report outputs (`test-output/`, `testng-results.xml`, `testng-failed.xml`, `emailable-report.html`) as local test report artifacts, with matching root and nested `.gitignore` coverage for `test-output/` and root `.gitignore` coverage for generated TestNG report files.
- Additional current public risk-scan scope: VSTest/.NET test result outputs (`TestResults/`, `*.trx`, `*.coverage`, `*.coveragexml`) as local test report artifacts, with matching root and nested `.gitignore` coverage for `TestResults/` and root `.gitignore` coverage for generated report files.
- Additional current public risk-scan scope: NUnit XML report outputs (`nunit.xml`, `*.nunit.xml`, `TestResult.xml`, `TestResult-*.xml`) as local test report artifacts, with matching root `.gitignore` coverage for generated NUnit report files.
- Additional current public risk-scan scope: Go test runner report exports (`gotestsum.json`, `gotestsum-report.xml`, `go-test-report.json`, `go-test-results.xml`) as local test report artifacts, with matching root `.gitignore` coverage for generated Go test report files.
- Additional current public risk-scan scope: Allure report/result directories (`allure-report/`, `allure-results/`) as local test report artifacts, with matching root and nested `.gitignore` coverage.
- Additional current public risk-scan scope: Postman/Newman API test report outputs (`newman/`, `newman-report.html`, `newman-report.json`, `newman-report.xml`) as local test report artifacts, with matching root and nested `.gitignore` coverage for `newman/` and root `.gitignore` coverage for generated Newman report files.
- Additional current public risk-scan scope: generated load-test report outputs from k6, Artillery, and Locust (`k6-report/`, `k6-reports/`, `k6-report.html`, `k6-summary.json`, `artillery-report/`, `artillery-results.json`, `locust-report/`, `locust_stats.csv`) as local test report artifacts, with matching root and nested `.gitignore` coverage while preserving source-of-truth load-test definitions such as k6 scripts, Artillery YAML, and `locustfile.py`.
- Additional current public risk-scan scope: Robot Framework result/report outputs (`robot-results/`, `robot-reports/`, `robot-output.xml`, `robot-log.html`, `robot-report.html`) as local test report artifacts, with matching root and nested `.gitignore` coverage for directory-shaped Robot outputs and root `.gitignore` coverage for explicitly Robot-named report files while preserving generic default names outside Robot-specific directories.
- Additional current public risk-scan scope: Apache JMeter generated result/report outputs (`jmeter-report/`, `jmeter-reports/`, `jmeter-results/`, `*.jtl`) as local test report artifacts, with matching root and nested `.gitignore` coverage for directory-shaped JMeter outputs and root `.gitignore` coverage for generated JMeter result logs while preserving source-of-truth JMeter test plans such as `*.jmx`.
- Additional current public risk-scan scope: Jest generated report outputs (`jest-stare/`, `jest-html-reporters-attach/`, `jest_html_reporters.html`, `jest-junit.xml`, `jest-results.json`) as local test report artifacts, with matching root and nested `.gitignore` coverage for directory-shaped Jest reports and root `.gitignore` coverage for explicitly Jest-named report files while preserving source-of-truth Jest configuration such as `jest.config.js`.
- Additional current public risk-scan scope: Vitest generated report outputs (`vitest-report/`, `vitest-report.html`, `vitest-report.json`, `vitest-report-*` variants, `vitest-junit.xml`, `vitest-results.json`) as local test report artifacts, with matching root and nested `.gitignore` coverage for directory-shaped Vitest reports and root `.gitignore` coverage for explicitly Vitest-named report files while preserving source-of-truth Vitest configuration such as `vitest.config.js` and `vitest.config.ts`.
- Additional current public risk-scan scope: TAP generated report outputs (`tap-report/`, `tap-reports/`, `tap-report.html`, `tap-report.json`, `tap-report.xml`, `tap-report.tap`, `tap-results.tap`) as local test report artifacts, with matching root and nested `.gitignore` coverage for directory-shaped TAP reports and root `.gitignore` coverage for generated TAP report files while preserving arbitrary `*.tap` files outside explicit TAP report names or directories.
- Additional current public risk-scan scope: Maven Surefire/Failsafe report directories (`surefire-reports/`, `failsafe-reports/`) as local test report artifacts, with matching root and nested `.gitignore` coverage.
- Additional current public risk-scan scope: JaCoCo coverage outputs (`jacocoHtml/`, `jacoco.exec`, `jacoco.xml`, `jacoco.csv`) as local test report artifacts, with matching root and nested `.gitignore` coverage where directory-shaped.
- Additional current public risk-scan scope: Cobertura/OpenCover coverage XML outputs (`cobertura.xml`, `*.cobertura.xml`, `opencover.xml`, `*.opencover.xml`) as local test report artifacts, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: Istanbul/nyc coverage outputs (`.nyc_output/`, `coverage-final.json`, `coverage-summary.json`, `clover.xml`, and `*.clover.xml`) as local test report artifacts, with matching root and nested `.gitignore` coverage for `.nyc_output/` and root `.gitignore` coverage for generated Istanbul/nyc report files.
- Additional current public risk-scan scope: coverage.py JSON report output (`coverage.json`) as a local coverage artifact, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: coverage upload JSON exports (`codecov.json`, `coveralls.json`, `*.codecov.json`, `*.coveralls.json`) as local coverage artifacts, with matching root `.gitignore` coverage while preserving service configuration files such as `codecov.yml`, `.codecov.yml`, and `.coveralls.yml`.
- Additional current public risk-scan scope: cargo-tarpaulin coverage report outputs (`tarpaulin-report.html`, `tarpaulin-report.json`) as local test report artifacts, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: LCOV HTML report directories (`lcov-report/`) as local coverage report artifacts, with matching root and nested `.gitignore` coverage. The scanner rejects missing root and nested `.gitignore` coverage for those local coverage report artifacts.
- Additional current public risk-scan scope: Go coverage profile outputs (`cover.out`, `coverage.out`, `*.coverprofile`) as local Go cache/test artifacts, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: pytest generated report outputs (`.report.json`, `pytest-report.html`, `pytest-report.json`, `pytest-report.xml`, and `pytest-report-*` variants) as local test report artifacts, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: mutation-testing report/cache outputs (`.mutmut-cache/`, `.stryker-tmp/`, `mutation-report/`, `pit-reports/`, `mutation-report.html`, and `mutmut.sqlite`) as local test report artifacts, with matching root and nested `.gitignore` coverage.
- Additional current public risk-scan scope: CTRF generated test report outputs (`ctrf/`, `ctrf-report.json`, `ctrf-report-*.json`) as local test report artifacts, with matching root and nested `.gitignore` coverage for report directories and root `.gitignore` coverage for generated CTRF report files.
- Fresh-clone QA: passed for the launch baseline and earlier evidence commits. For `128550a`, focused fresh-clone QA passed after reducing cold-build pressure with `CARGO_BUILD_JOBS=1` and shared local target artifacts. Later optional API evidence, optional artifact-QA CI wiring, no-download refusal evidence, and public-contract QA hardening commits are covered by focused static/public-risk/artifact QA gates and preserved opt-in local artifact QA, not a new full fresh-clone run.
- Public risk-scan hardening detail: PID/process marker files (`*.pid`), DuckDB databases/WAL files (`*.duckdb`, `*.duckdb.wal`), Redis RDB snapshots (`*.rdb`, `dump.rdb`), patch/diff files (`*.patch`, `*.diff`), local editor history directories (`.history/`), Python Hypothesis example databases (`.hypothesis/`), Python coverage data files (`.coverage`, `.coverage.*`), pytest generated report outputs (`.report.json`, `pytest-report.html`, `pytest-report.json`, `pytest-report.xml`, and `pytest-report-*` variants), NYC/Istanbul coverage artifacts such as `.nyc_output/`, `coverage-final.json`, `coverage-summary.json`, `clover.xml`, and `*.clover.xml`, coverage.py JSON report output (`coverage.json`), coverage upload JSON exports (`codecov.json`, `coveralls.json`, `*.codecov.json`, `*.coveralls.json`), JaCoCo coverage outputs (`jacocoHtml/`, `jacoco.exec`, `jacoco.xml`, `jacoco.csv`), Cobertura coverage XML output (`cobertura.xml`), VSTest/.NET test result outputs (`TestResults/`, `*.trx`, `*.coverage`, `*.coveragexml`), TAP generated report outputs (`tap-report/`, `tap-reports/`, `tap-report.html`, `tap-report.json`, `tap-report.xml`, `tap-report.tap`, `tap-results.tap`), local Python package-manager/dependency artifacts (`.pdm-build/`, `__pypackages__/`, `.uv/`, `.uv-cache/`, `.pip-cache/`, `uv-cache/`, `pip-cache/`), Ruby/Bundler local dependency artifacts (`.bundle/`, `vendor/bundle/`, `vendor/cache/`), R/RStudio local session/dependency artifacts (`.Rproj.user/`, `.Rhistory`, `.RData`, `.Ruserdata`, `renv/library/`), Julia depot/preference artifacts (`.julia/`, `LocalPreferences.toml`, `*.jl.cov`, `*.jl.mem`), Go cache/test generated outputs (`.gocache/`, `.gomodcache/`, `cover.out`, `coverage.out`, `*.coverprofile`, `*.test`), Elixir/Mix build/dependency outputs (`.elixir_ls/`, `_build/`, `deps/`), Erlang/Rebar3 cache/crash outputs (`.rebar3/`, `rebar3.crashdump`, `erl_crash.dump`), JVM compiler bytecode outputs (`*.class`, `*.tasty`), .NET/NuGet dependency/build/user-state artifacts (`.nuget/`, `packages/`, `bin/Debug/`, `obj/Release/`, `project.assets.json`, `project.nuget.cache`, `*.nupkg`, `*.snupkg`, `*.csproj.user`, `*.suo`), JVM package archives (`*.jar`, `*.war`, `*.ear`), Haskell Stack/Cabal build outputs (`.stack-work/`, `dist-newstyle/`, `.cabal-sandbox/`, `cabal.sandbox.config`), OCaml/opam local switch artifacts (`_opam/`, `.opam-switch/`), Lua/LuaRocks local dependency/build/package outputs (`.luarocks/`, `lua_modules/`, `*.rock`), Zig build outputs (`.zig-cache/`, `zig-cache/`, `zig-out/`), Autotools configure/Libtool outputs (`autom4te.cache/`, `.deps/`, `.libs/`, `config.log`, `config.status`, generated `libtool` files, `*.lo`, `*.la`), ML experiment/tracking outputs (`.wandb/`, `wandb/`, `mlruns/`, `lightning_logs/`, `events.out.tfevents.*`), LCOV coverage outputs (`lcov.info`, `*.lcov`) and LCOV HTML report directories (`lcov-report/`), SARIF/code-scanning reports (`*.sarif`, `*.sarif.json`), SonarQube/SonarScanner local analysis outputs (`.scannerwork/`, `.sonar/`), Windows Explorer thumbnail caches (`ehthumbs.db`) and recycle-bin metadata (`$RECYCLE.BIN/`), JetBrains/Eclipse/Zed IDE artifacts (`*.iml`, `.classpath`, `.project`, `.settings/`, `.zed/`), TLS certificate/request artifacts (`*.crt`, `*.csr`, `*.der`, `*.p7b`, `*.p7c`), app-signing key stores (`*.p8`, `*.jks`, `*.keystore`), Expo local state (`.expo/`, `.expo-shared/`), Android native build intermediates (`.cxx/`, `.externalNativeBuild/`), CocoaPods dependency outputs (`Pods/`, `ios/Pods/`), Carthage dependency outputs (`Carthage/Build/`, `Carthage/Checkouts/`), Fastlane generated report/test artifacts (`fastlane/report.xml`, `fastlane/Preview.html`, `fastlane/test_output/`), mobile provisioning/build archives (`*.mobileprovision`, `*.provisionprofile`, `*.xcarchive`), Swift Package Manager build/workspace artifacts (`.build/`, `.swiftpm/`), Bazel build outputs (`bazel-bin/`, `bazel-out/`, `bazel-testlogs/`, root `bazel-*`), Playwright/browser-test report directories (`playwright-report/`, `blob-report/`, `.playwright/`), Cypress browser-test screenshot/video/download directories (`cypress/screenshots/`, `cypress/videos/`, `cypress/downloads/`), Mochawesome test report outputs (`mochawesome-report/`, `mochawesome.html`, `mochawesome.json`), Cucumber/BDD test report outputs (`cucumber-report/`, `cucumber-reports/`, `cucumber-report.html`, `cucumber-report.json`, `cucumber-report.xml`), frontend lint cache files (`.eslintcache`, `.stylelintcache`), Bun cache/debug-log artifacts (`.bun/`, `bun-debug.log`), local cache directories (`.cache/`), temporary scratch directories (`tmp/`, `temp/`) and files (`*.tmp`, `*.temp`), Jupyter runtime/config artifacts (`.jupyter/`, `.nbhistory`), Terraform/OpenTofu/Terragrunt state artifacts (`.terraform/`, `.tofu/`, and `.terragrunt-cache/` at any tree depth, `.terraform.lock.hcl`, `.tofu.lock.hcl`, `.tfstate`, `.tfvars`, `.tfvars.json`, `.tfplan`), deployment platform local state directories (`.vercel/`, `.netlify/`, `.wrangler/`) and Cloudflare Workers local secret/config files (`.dev.vars`, `.dev.vars.*`) at any tree depth, native/CMake build artifacts, default screenshot/screen-recording captures, and audio/video capture exports are blocked as tracked local artifacts, with matching root and nested `.gitignore` coverage where the scanner rejects nested local-state directories.
- The local documentation build artifact guard rejects tracked LaTeX/Pandoc-adjacent build byproducts such as `*.aux`, `*.bbl`, `*.blg`, `*.fdb_latexmk`, `*.fls`, `*.synctex.gz`, `_minted-*` caches, and generated API documentation exports such as `swagger-ui/`, `swagger-ui-dist/`, `redoc-static/`, `openapi-generated/`, `api-docs-generated/`, `swagger-ui.html`, `redoc-static.html`, and `openapi-bundle.json`, and rejects missing root and nested `.gitignore` coverage for local documentation build artifacts. It avoids a blanket PDF block and preserves source-of-truth API specification/configuration files such as `openapi.yaml`, `openapi.json`, `.redocly.yaml`, `redocly.yaml`, `openapitools.json`, and `openapi-generator-config.*` so public docs inputs are not rejected solely by extension or tool ownership.
- The local Python cache/build and virtualenv/dependency artifact guards reject local Python caches, package metadata/build outputs, virtualenvs, and package-manager cache directories while preserving source-of-truth Python files such as `pyproject.toml`, `setup.py`, `setup.cfg`, `requirements*.txt`, `Pipfile`, `Pipfile.lock`, `poetry.lock`, `uv.lock`, Python source files, and Python docs by filename alone.
- The local ML experiment/tracking artifact guard rejects tracked W&B, MLflow, Lightning, and TensorBoard run outputs such as `.wandb/`, `wandb/`, `mlruns/`, `lightning_logs/`, and `events.out.tfevents.*`, and rejects missing root and nested `.gitignore` coverage for local ML experiment/tracking artifacts.
- The local Meson build artifact guard rejects tracked local Meson build artifacts such as `.mesonpy-*` build directories at any tree depth, `builddir/`, `meson-info/`, `meson-logs/`, and `meson-private/`, and rejects missing root and nested `.gitignore` coverage for local Meson build artifacts while preserving source-of-truth Meson files such as `meson.build`, `meson_options.txt`, and `meson.options`.
- The local Autotools build artifact guard rejects tracked local Autotools configure and Libtool outputs such as `autom4te.cache/`, `.deps/`, `.libs/`, `config.log`, `config.status`, generated `libtool` files, `*.lo`, and `*.la`, rejects missing root `.gitignore` coverage for those local Autotools outputs, and preserves source-of-truth Autotools files such as `configure.ac`, `Makefile.am`, and Autotools docs.
- The local Nix build result artifact guard rejects tracked local Nix build result artifacts such as root `result` and `result-*` outputs, rejects missing root `.gitignore` coverage for local Nix build result artifacts, and preserves source-of-truth Nix files such as `flake.nix`, `flake.lock`, `default.nix`, `shell.nix`, and `*.nix` files.
- The local Bazel build artifact guard rejects tracked local Bazel build artifacts such as `bazel-bin/`, `bazel-out/`, `bazel-testlogs/`, and root `bazel-*` outputs, and rejects missing root `.gitignore` coverage for local Bazel build artifacts while preserving source-of-truth Bazel files such as `BUILD`, `BUILD.bazel`, `MODULE.bazel`, and `.bzl` files.
- The local Buck/Buck2 build artifact guard rejects tracked local Buck/Buck2 build artifacts such as `.buckd/` and `buck-out/`, and rejects missing root and nested `.gitignore` coverage for local Buck/Buck2 build artifacts while preserving source-of-truth Buck files such as `BUCK`, `BUCK.v2`, and `.buckconfig`.
- The local Swift Package Manager artifact guard rejects tracked local Swift Package Manager build/workspace artifacts such as `.build/` and `.swiftpm/` at any tree depth, and rejects missing root and nested `.gitignore` coverage for local Swift Package Manager artifacts while preserving source-of-truth Swift package files such as `Package.swift` and Swift source files.
- The local Zig build artifact guard rejects tracked local Zig build artifacts such as `.zig-cache/`, `zig-cache/`, and `zig-out/` at any tree depth, and rejects missing root and nested `.gitignore` coverage for local Zig build artifacts while preserving source-of-truth Zig files such as `build.zig`, `build.zig.zon`, and Zig source files.
- The local Dart/Flutter artifact guard rejects tracked local Dart/Flutter artifacts such as `.dart_tool/`, `.pub-cache/`, `.pub/`, `.packages`, `.flutter-plugins`, and `.flutter-plugins-dependencies` at any tree depth, and rejects missing root and nested `.gitignore` coverage for local Dart/Flutter artifacts while preserving source-of-truth project files such as `pubspec.yaml`, `pubspec.lock`, Dart source, Android Gradle project files, and iOS Xcode project files.
- The local native/CMake build artifact guard rejects tracked local native/CMake build/user-local artifacts such as `cmake-build-*`, `CMakeFiles/` at any tree depth, CTest `Testing/Temporary/` output, CPack staging output such as `_CPack_Packages/`, generated CTest/Dart files such as `CTestTestfile.cmake` and `DartConfiguration.tcl`, generated CPack files such as `CPackConfig.cmake` and `CPackSourceConfig.cmake`, `CMakeCache.txt`, user-local `CMakeUserPresets.json`, `cmake_install.cmake`, `compile_commands.json`, `install_manifest.txt`, and Ninja local state files such as `.ninja_deps` and `.ninja_log`, while preserving source-of-truth CMake files such as `CMakeLists.txt` and `CMakePresets.json`, and rejects missing root and nested `.gitignore` coverage for local native/CMake build artifacts.
- The local mobile/Xcode/Android build artifact guard rejects missing root, nested, Android-specific, and platform artifact `.gitignore` coverage for local mobile build outputs while preserving source-of-truth app and build configuration files.
- Local CI runner hardening detail: local `act` workflow runner state/config (`.act/` and `.actrc`) and GitHub Actions self-hosted runner state/config (`actions-runner/`, `_work/`, `_diag/`, `.runner`, `.credentials`, and `.credentials_rsaparams`) are blocked as tracked launch-facing material, with matching root and nested `.gitignore` coverage.
- The Ruby/Bundler dependency artifact guard rejects tracked local Bundler state/cache paths such as `.bundle/`, `vendor/bundle/`, and `vendor/cache/` at any tree depth, and rejects missing root `.gitignore` coverage for local Ruby/Bundler dependency artifacts plus missing nested `.gitignore` coverage while preserving source-of-truth Ruby files such as `Gemfile`, `Gemfile.lock`, Ruby source files, Ruby docs, and non-artifact vendor paths.
- The PHP Composer dependency/test artifact guard rejects tracked local Composer/PHPUnit artifacts such as `vendor/autoload.php`, `vendor/bin/`, `vendor/composer/`, `.phpunit.cache/`, `.phpunit.result.cache`, and `composer.phar`, including Composer vendor/PHPUnit cache artifacts at any tree depth, and rejects missing root `.gitignore` coverage for local PHP Composer dependency/test artifacts plus missing nested `.gitignore` coverage while preserving source-of-truth Composer files such as `composer.json`, `composer.lock`, PHP source files, and PHP docs.
- The Perl/CPAN build/dependency artifact guard rejects tracked local CPAN client, build, and local::lib artifacts such as `.cpan/`, `.cpanm/`, `blib/`, `local/lib/perl5/`, `MYMETA.json`, `MYMETA.yml`, and `pm_to_blib`, rejects missing root and nested `.gitignore` coverage for local Perl/CPAN build/dependency artifacts, and preserves source-of-truth Perl files such as `cpanfile`, `cpanfile.snapshot`, `Makefile.PL`, `Build.PL`, Perl source files, and Perl docs.
- The R/RStudio artifact guard rejects tracked local R/RStudio artifacts such as `.Rproj.user/` and `renv/library/` at any tree depth, plus `.Rhistory`, `.RData`, and `.Ruserdata`, rejects missing root and nested `.gitignore` coverage for local R/RStudio artifacts, and preserves source-of-truth R files such as `*.R`, `*.Rmd`, `.Rproj` project files, `renv.lock`, `DESCRIPTION`, and R docs.
- The Julia depot/preference artifact guard rejects tracked local Julia depot/preference artifacts such as `.julia/` at any tree depth, `LocalPreferences.toml`, `*.jl.cov`, and `*.jl.mem`, and rejects missing root and nested `.gitignore` coverage for local Julia depot/preference artifacts while preserving source-of-truth Julia project files such as `Project.toml`, `Manifest.toml`, `Artifacts.toml`, and Julia source files by filename alone.
- The Go cache/test artifact guard rejects tracked local Go cache/test artifacts such as `.gocache/` and `.gomodcache/` at any tree depth, `cover.out`, `coverage.out`, `*.coverprofile`, and `*.test`, rejects missing root and nested `.gitignore` coverage for local Go cache/test artifacts, and preserves source-of-truth Go files such as `go.mod`, `go.sum`, Go source files, and Go docs.
- The Elixir/Mix build/dependency artifact guard rejects tracked local Elixir/Mix build/dependency artifacts such as `.elixir_ls/`, `_build/`, and `deps/` at any tree depth, rejects missing root and nested `.gitignore` coverage for local Elixir/Mix build/dependency artifacts, and preserves source-of-truth Elixir/Mix files such as `mix.exs`, `mix.lock`, Elixir source files, and Elixir docs.
- The Erlang/Rebar3 artifact guard rejects tracked local cache and crash artifacts such as `.rebar3/` at any tree depth, `rebar3.crashdump`, and `erl_crash.dump`, and rejects missing root and nested `.gitignore` coverage for local Erlang/Rebar3 artifacts while preserving source-of-truth Erlang/Rebar3 files such as `rebar.config`, `rebar.lock`, Erlang source files, and Erlang header files.
- The JVM dependency artifact guard rejects tracked local JVM dependency artifacts such as `.m2/` at any tree depth, and rejects missing root and nested `.gitignore` coverage for local JVM dependency artifacts while preserving source-of-truth Maven/JVM project files such as `pom.xml`, Java source files, Kotlin source files, Scala source files, and JVM docs.
- The JVM compiler artifact guard rejects tracked local bytecode outputs such as `*.class` and `*.tasty`, and rejects missing root and nested `.gitignore` coverage for local JVM compiler artifacts while preserving source-of-truth JVM project files such as `pom.xml`, `build.gradle`, `build.sbt`, Java source files, Kotlin source files, and Scala source files.
- The .NET/NuGet artifact guard rejects tracked local dependency/build/user-state artifacts such as `.nuget/`, `packages/`, `bin/Debug/`, `obj/Release/`, `project.assets.json`, `project.nuget.cache`, `*.nupkg`, `*.snupkg`, `*.csproj.user`, and `*.suo`, and rejects missing root `.gitignore` coverage for local .NET/NuGet artifacts while preserving source-of-truth .NET project files such as `*.csproj`, `*.fsproj`, `*.vbproj`, `*.sln`, `Directory.Build.props`, C# source files, F# source files, and NuGet lock files.
- The Clojure/Leiningen artifact guard rejects tracked local artifacts such as `.lein/`, `.cpcache/`, and `.shadow-cljs/` at any tree depth, plus `.nrepl-port`, and rejects missing root and nested `.gitignore` coverage for local Clojure/Leiningen artifacts while preserving source-of-truth Clojure files such as `project.clj`, `deps.edn`, `bb.edn`, `shadow-cljs.edn`, and Clojure/ClojureScript source files.
- The Gradle/JVM build artifact guard rejects tracked local Gradle cache/state and build outputs such as `.gradle/`, `examples/android/.gradle/`, `app/build/classes/`, `service/build/reports/`, `service/build/test-results/`, `service/build/tmp/`, `service/build/generated/`, and `service/build/libs/`, and rejects missing root and nested `.gitignore` coverage for local Gradle/JVM cache/state artifacts.
- The local Kotlin/Kotlin Native compiler artifact guard rejects tracked local Kotlin/Kotlin Native compiler artifacts such as `.kotlin/` and `.konan/` at any tree depth, and rejects missing root and nested `.gitignore` coverage for local Kotlin/Kotlin Native artifacts while preserving source-of-truth Kotlin files such as `build.gradle.kts`, `settings.gradle.kts`, Kotlin source files, and Gradle version catalog files.
- The Scala/SBT build artifact guard rejects tracked local Scala build server/IDE outputs such as `.bloop/`, `.bsp/`, `.metals/`, and `.scala-build/` at any tree depth, and rejects missing root and nested `.gitignore` coverage for local Scala/SBT build artifacts while preserving source-of-truth Scala/SBT files such as `build.sbt`, `project/build.properties`, and Scala source files.
- The Haskell Stack/Cabal build artifact guard rejects tracked local build outputs such as `.stack-work/`, `dist-newstyle/`, `.cabal-sandbox/`, and `cabal.sandbox.config` at any tree depth, and rejects missing root and nested `.gitignore` coverage for local Haskell Stack/Cabal build artifacts while preserving source-of-truth Haskell files such as `.cabal` files, `stack.yaml`, `cabal.project`, and Haskell source files by filename alone.
- The local OCaml/opam artifact guard rejects tracked local OCaml/opam local switch artifacts such as `_opam/` and `.opam-switch/` at any tree depth, and rejects missing root and nested `.gitignore` coverage for local OCaml/opam artifacts while preserving source-of-truth OCaml project files such as `dune`, `dune-project`, `*.opam`, and OCaml source files.
- The Lua/LuaRocks artifact guard rejects tracked local Lua/LuaRocks dependency/build/package artifacts such as `.luarocks/`, `lua_modules/`, and `*.rock` at any tree depth, and rejects missing root and nested `.gitignore` coverage for local Lua/LuaRocks artifacts while preserving source-of-truth Lua files such as `*.lua`, `*.rockspec`, LuaRocks lock files, and Lua config files.
- The Python cache/build artifact guard rejects tracked local Pyre/Pytype and mypy daemon state such as `.pyre/`, `.pytype/`, and `.dmypy.json`, plus Python package metadata/build artifacts such as `.eggs/`, `*.egg-info/`, and `*.dist-info/`, and rejects missing root and nested `.gitignore` coverage for local Python cache/build artifacts. The Python virtualenv/dependency artifact guard also rejects local PDM/PEP 582 artifacts such as `.pdm-build/` and `__pypackages__/`, plus virtualenv and package-manager cache directories at any tree depth, and rejects missing root and nested `.gitignore` coverage for those local Python dependency/build artifacts.
- The TypeScript type-check report artifact guard rejects tracked generated reports such as `tsc-report/`, `tsc-report.txt`, `typecheck-report/`, `typecheck-results.json`, `typescript-report.xml`, and timestamped `tsc-report-*`, `typecheck-results-*`, or `typescript-results-*` files, with matching root and nested `.gitignore` coverage. It does not treat source-of-truth TypeScript project configuration such as `tsconfig.json`, `tsconfig.*.json`, frontend package manifests, JavaScript source files, or TypeScript source files as local report artifacts.
- The Python benchmark artifact guard rejects tracked local pytest-benchmark artifacts such as `.benchmarks/` at any tree depth and `pytest-benchmark.json` exports, rejects missing root and nested `.gitignore` coverage for local Python benchmark artifacts, while preserving curated benchmark evidence under `docs/benchmarks/` and source helpers such as `scripts/bench_backend.py`.
- Dependency lockfile source hardening includes Python `Pipfile.lock`, `poetry.lock`, and `uv.lock` local source fixtures, plus Yarn `portal:` dependencies and local Yarn `patch:` references, so those cannot quietly smuggle local/private source paths into public launch evidence.

## Gates represented by the snapshot

The referenced fresh-clone QA gates verified:

- exact `origin/main` checkout and clean initial/final status;
- no repo-local `AGENTS.md`;
- no tracked files larger than 1 MiB;
- public risk scan and focused sensitive/overclaim review;
- JSON, Python, shell, static QA, API-example regression, and artifact-QA gates;
- `cargo fmt --all --check` and `cargo test -q`;
- manifest-driven public contract smoke in default and artifact modes;
- public-contract smoke artifact summaries accepted by `scripts/public_contract_smoke_artifact_qa.py`;
- manifest shape validation, manifest-to-`/v1` docs boundary coverage, and manifest `base_url` alignment across public docs/examples in `scripts/public_api_contract_qa.py`;
- MiniLM, SmolLM2, and Qwen2.5 optional API acceptance artifact schemas covered by offline self-tests in default CI;
- lockfile-reproducible frontend clean install plus frontend build/copy QA.

## What this evidence proves

- The documented no-download public `/v1` routing/refusal contract works against the real backend process in isolated empty state.
- Public-contract smoke summary artifacts have an offline QA gate for schema, pass/fail semantics, coverage, caveats, and share-safety.
- Public-contract smoke summary artifacts reject duplicate endpoint, boundary, and deferred-boundary entries so summary coverage cannot be inflated by repeated rows.
- Public-contract smoke summary artifacts keep Markdown endpoint/boundary/deferred/status/proof-scope rows aligned with JSON evidence rows.
- The backend acceptance artifact summary share-safety guard rejects local paths, secret markers, and request/payload text in shareable `summary.json` and `summary.md` evidence before those summaries are treated as public handoff artifacts.
- The offline public API contract QA gate validates manifest shape, keeps manifest boundary names covered by the `/v1` docs and refusal matrix, and requires request hints for status/code refusal boundaries to be exposed in the refusal matrix.
- The offline public API contract QA gate keeps the local API no-auth, loopback-only, internet/untrusted-LAN exposure, and `SECURITY.md` review warnings attached to the README, backend quickstart, `/v1` contract, and public launch checklist.
- The offline public API contract QA gate keeps repository-level MIT license metadata aligned across Cargo manifests, README, and `LICENSE`.
- The offline public API contract QA gate keeps root `.gitattributes` text-normalization metadata present, including binary classifications for common model, package, mobile, media, and local plan artifacts, so public diffs remain stable across platforms without line-ending normalization of binary evidence.
- The offline public API contract QA gate keeps every tracked Python helper covered by the documented and CI `python3 -m py_compile` syntax gate and every tracked shell helper covered by the documented and CI `bash -n` syntax gate, including local oracle helpers that are not run during no-download launch QA.
- The offline public API contract QA gate keeps the public launch clean-clone frontend install on `npm --prefix frontend ci`, so release handoffs use the checked-in lockfile rather than refreshing dependency resolution with `npm install`.
- The offline public API contract QA gate keeps CONTRIBUTING and the pull request template aligned with the launch checklist on Python and shell syntax gates, so contributors and reviewers do not lose the syntax checks when using common gates or PR checkboxes.
- The offline API client example regression gate runs the OpenAI SDK example through a dependency-free local `openai` stub and checks both request routing/body shape and the JSON response payloads printed by the example, including opt-in float embeddings.
- The offline API client example regression gate keeps the REST Client `.http` example on the expected request sequence and requires JSON `Content-Type` headers on every documented POST block, with synthetic negative self-tests for missing headers, request-order drift, streaming chat, and non-float encoding drift.
- The public API contract QA self-test exercises synthetic refused/unsupported public overclaim examples for streaming, external proxying, OpenAI parity, SafeTensors blanket execution, base64 embeddings, production readiness, and legal/license suitability, plus allowed caveated examples.
- The offline public API contract QA gate keeps public issue-template intake fields and privacy/safety checkbox options required, so API contract, model/runtime, bug, and security/privacy reports cannot silently become optional public artifact-disclosure prompts.
- The offline public API contract QA and CI static policy gates require default CI to run both the public risk scan self-test and the tracked-file public risk scan, while preserving the public caveat that the scan is not a complete privacy audit.
- The public risk scan rejects obvious tracked secret-token values such as OpenAI-style, GitHub, Hugging Face, Slack, AWS access key IDs, Google API keys, and bearer tokens while allowing placeholder API-key copy. It also rejects PEM private-key block headers while allowing public-key headers, macOS, Linux, and Windows home/profile paths, dependency lockfile local file/path sources, SSH-only dependency sources, authenticated dependency URLs, secret values, private local paths, tracked Git submodule metadata such as `.gitmodules` local/relative, SSH-only, authenticated, secret, or private source URLs, tracked Git LFS pointer files that can hide external artifact downloads, tracked cloud SDK credential/config files such as `.aws/`, `.azure/`, `.config/gcloud/`, `.boto`, `boto.cfg`, `application_default_credentials.json`, `service-account.json`, `service_account.json`, and `serviceAccountKey.json`, missing root and nested `.gitignore` coverage for local cloud SDK credential/config files, tracked Kubernetes credential/config files such as `.kube/`, `kubeconfig`, `kubeconfig.yaml`, and `kubeconfig.yml`, missing root and nested `.gitignore` coverage for local Kubernetes credential/config files, tracked OS/platform metadata files such as `.DS_Store`, `Thumbs.db`, `ehthumbs.db`, `desktop.ini`, `$RECYCLE.BIN/`, `__MACOSX/`, `._*`, `.AppleDouble`, `.fseventsd/`, `.Spotlight-V100/`, `.TemporaryItems/`, `.Trashes/`, `.LSOverride`, and `.localized`, missing root `.gitignore` coverage for local OS/platform metadata files, tracked editor backup/swap artifacts, tracked local editor history artifacts such as `.history/`, tracked patch/diff artifacts, missing root `.gitignore` coverage for local editor backup/swap files, missing root `.gitignore` coverage for local editor history directories, missing root `.gitignore` coverage for local patch/diff files, tracked IDE workspace/config artifacts, missing root and nested `.gitignore` coverage for local IDE workspace/config artifacts, tracked credential/config filenames including `.envrc`, Git credential/config files such as `.git-credentials`, `.gitconfig`, and `.gitconfig.local`, SSH private-key filenames such as `id_rsa`, `id_ed25519`, `id_ecdsa`, and `id_dsa`, tracked `.direnv/`, `.ssh/`, `secrets/`, and `private/` directories, tracked generic secret material files such as `secrets.json`, `secrets.yaml`, `secrets.yml`, `*.secret`, and `*.secrets`, plus TLS certificate/request artifacts such as `*.crt`, `*.csr`, `*.der`, `*.p7b`, and `*.p7c`, missing root and nested `.gitignore` coverage for local credential/config files including `.git-credentials`, `.gitconfig`, `.gitconfig.local`, `/.ssh/`, SSH private-key filenames, `/.direnv/`, `.envrc`, `/secrets/`, `/private/`, and generic secret material patterns and TLS certificate/request artifact patterns, tracked workspace/personal agent context files including local assistant state such as `.codex/`, `.claude/`, `.continue/`, `.cursor/`, `.windsurf/`, and `.aider.*`, missing root and nested `.gitignore` coverage for personal workspace context including local assistant state, tracked local shell/REPL command history files such as `.bash_history`, `.zsh_history`, `.python_history`, `.node_repl_history`, `.psql_history`, `.sqlite_history`, `.mysql_history`, `.rediscli_history`, and `.fish_history`, missing root and nested `.gitignore` coverage for local shell/REPL command history files, tracked local runtime/artifact detail files, missing root and nested `.gitignore` coverage for local runtime/artifact detail files, tracked local log/trace/profiling/debug-output artifacts such as top-level `logs/`, `traces/`, `profiles/`, `debug-output/`, `.trace`, `.cpuprofile`, `.heapsnapshot`, `.perf`, `.prof`, `.sarif`, `.sarif.json`, `.core`, `.crash`, `.dmp`, `.ips`, and root `core` files, missing root `.gitignore` coverage for local log/trace/profiling/debug-output artifacts including `*.log`, SARIF/code-scanning reports, and crash dump patterns, tracked Python cache/build artifacts such as `.hypothesis/`, `__pycache__/`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, and `.pyc`/`.pyo` files, missing root and nested `.gitignore` coverage for local Python cache/build artifacts, tracked Python virtualenv/dependency artifacts such as `.venv/`, `venv/`, `env/`, `.tox/`, `.nox/`, `wheelhouse/`, `pip-wheel-metadata/`, and `site-packages/`, missing root and nested `.gitignore` coverage for local Python virtualenv/dependency artifacts, tracked frontend/Node cache/build artifacts including root-level `dist/` or `build/` output, Bun package-manager caches/logs, Vite/Vitest cache/config timestamp artifacts, and lint cache files such as `.eslintcache` and `.stylelintcache`, missing root and nested `.gitignore` coverage for local frontend/Node cache/build artifacts, tracked local cache artifacts such as `.cache/`, missing root `.gitignore` coverage for local cache artifacts, tracked local temporary/scratch artifacts such as `tmp/`, `temp/`, `*.tmp`, and `*.temp`, missing root and nested `.gitignore` coverage for local temporary/scratch artifacts, tracked local test report/coverage artifacts such as `test-results/`, `test-reports/`, `reports/`, `htmlcov/`, `.coverage`, `.coverage.*`, `coverage.xml`, `lcov.info`, `*.lcov`, `junit.xml`, and `*.junit.xml`, missing root and nested `.gitignore` coverage for local test report/coverage artifacts, Playwright/browser-test report directories such as `playwright-report/`, `blob-report/`, and `.playwright/`, tracked local notebook checkpoint artifacts such as `.ipynb_checkpoints/`, tracked local notebook runtime/config artifacts such as `.jupyter/` and `.nbhistory`, tracked notebook execution outputs, missing root and nested `.gitignore` coverage for local notebook artifacts, tracked Rust/Cargo cache/build artifacts, missing root and nested `.gitignore` coverage for local Rust/Cargo cache/build artifacts, tracked local native/CMake build artifacts such as `cmake-build-*`, `CMakeFiles/`, `CMakeCache.txt`, `cmake_install.cmake`, and `compile_commands.json`, missing root `.gitignore` coverage for local native/CMake build artifacts, tracked release/package artifacts such as `.deb`, `.rpm`, `.msi`, `.rar`, `.zst`, `.jar`, `.war`, and `.ear`, missing root `.gitignore` coverage for local release/package artifacts, tracked backup/dump artifacts, missing root `.gitignore` coverage for local backup/dump artifacts, tracked local model/checkpoint artifacts such as top-level `models/`, `model-store/`, `weights/`, `checkpoints/`, `.safetensors`, `.gguf`, `.onnx`, `.bin`, `.pt`, `.pth`, `.ckpt`, `.npz`, `.npy`, and `.tflite` files, missing root `.gitignore` coverage for local model/checkpoint artifacts, tracked local Docker/container artifacts such as top-level `.docker/`, `docker-data/`, `docker-volumes/`, `docker-compose.override.yml`, `docker-compose.override.yaml`, `compose.override.yml`, and `compose.override.yaml`, missing root `.gitignore` coverage for local Docker/container artifacts, tracked local deployment platform artifacts such as `.vercel/`, `.netlify/`, `.wrangler/`, `.dev.vars`, and `.dev.vars.*`, missing root and nested `.gitignore` coverage for local deployment platform artifacts, tracked local Terraform/OpenTofu/Terragrunt infrastructure state artifacts such as `.terraform/`, `.tofu/`, `.terragrunt-cache/`, `.terraform.lock.hcl`, `.tofu.lock.hcl`, `.tfstate`, `.tfvars`, `.tfvars.json`, and `.tfplan` files, missing root and nested `.gitignore` coverage for local infrastructure state artifacts, tracked local mobile/Xcode/Android build artifacts such as `DerivedData/`, `.gradle/`, `xcuserdata/`, `local.properties`, `.xcuserstate`, `.xcresult`, `.ipa`, `.apk`, `.aab`, and `.dSYM` files, missing root `.gitignore` coverage for local mobile/Xcode/Android build artifacts, tracked local screenshot/screen-recording artifacts with default macOS capture prefixes, missing root `.gitignore` coverage for local screenshot/screen-recording artifacts, tracked local audio/video capture/export artifacts with common media extensions and default voice memo/audio recording prefixes, missing root `.gitignore` coverage for local audio/video capture/export artifacts, tracked symlinks that escape the repository or resolve only to missing/untracked local targets, and tracked files larger than 1 MiB so launch-facing checks catch accidental machine-local noise or large artifacts before publication.
- The local mobile/Xcode/Android artifact guard preserves source-of-truth mobile files such as `Package.swift`, `Podfile`, `Podfile.lock`, `*.xcodeproj/project.pbxproj`, Android Gradle build files, `AndroidManifest.xml`, Fastlane configuration, app-store metadata, and mobile docs while continuing to reject local DerivedData, native build intermediates, dependency output directories, app/archive packages, provisioning profiles, test result bundles, Fastlane reports, screenshots, and media captures.
- The Rust/Cargo guard rejects tracked Rust/Cargo cache/build artifacts such as `.cargo/`, `target/`, compiler outputs, and coverage profiles, with matching root and nested `.gitignore` coverage.
- The SCons guard rejects tracked configuration/cache outputs such as `.sconf_temp/`, `sconf_temp/`, `.sconsign`, and `.sconsign.dblite`, with matching root and nested `.gitignore` coverage while preserving `SConstruct` and `SConscript` as source-of-truth build files.
- The ML experiment/tracking artifact guard rejects tracked W&B, MLflow, Lightning, and TensorBoard run outputs such as `.wandb/`, `wandb/`, `mlruns/`, `lightning_logs/`, and `events.out.tfevents.*`, with matching root and nested `.gitignore` coverage.
- The frontend/Node cache guard includes package-manager caches/stores such as `.bun/`, `.npm/`, `.pnpm-store/`, `.yarn/cache/`, `.yarn/unplugged/`, `.yarn/build-state.yml`, and `.yarn/install-state.gz`, plus Bun debug logs, Vite/Vitest cache/config timestamp artifacts, Storybook static build output, Astro/Docusaurus/Hugo/Jekyll/VitePress/Metro/Nx/SWC/Next.js/SvelteKit/Nuxt/Angular framework cache/build outputs such as `.astro/`, `.docusaurus/`, `resources/_gen/`, `.hugo_build.lock`, `_site/`, `.jekyll-cache/`, `.sass-cache/`, `.vite/`, `.vitepress/cache/`, `.vitepress/dist/`, `.metro-cache/`, `.nx/`, `.swc/`, Rollup TypeScript plugin `.rpt2_cache/`, Webpack `.webpack-cache/`, `.next/`, `.svelte-kit/`, `.nuxt/`, `.output/`, and `.angular/cache/`, and lint cache files such as `.eslintcache` and `.stylelintcache`, with matching root and nested `.gitignore` coverage for selected nested Yarn, VitePress, Vite, Angular, Nx, and SWC cache/state paths while preserving source-of-truth lockfiles, package manifests, and Metro configuration such as `metro.config.js`.
- The frontend bundle-analysis guard rejects tracked local analyzer exports such as `bundle-analyzer-report.html`, `bundle-analysis.json`, `bundle-stats.html`, `webpack-stats.json`, `rollup-stats.html`, `vite-bundle-visualizer.html`, and name-suffixed variants, rejects missing root and nested `.gitignore` coverage for those generated reports, and preserves source-of-truth bundler configuration and package metadata such as `webpack.config.*`, `rollup.config.*`, `vite.config.*`, and frontend package manifests.
- The Deno local cache artifact guard rejects tracked local Deno cache artifacts such as `.deno/` and `deno-dir/`, and rejects missing root and nested `.gitignore` coverage for local Deno cache artifacts while preserving source-of-truth Deno project files such as `deno.json`, `deno.jsonc`, `deno.lock`, JavaScript files, and TypeScript files.
- The local Lighthouse/LHCI audit-output guard rejects tracked Lighthouse/LHCI audit outputs such as `.lighthouseci/`, `lhci_reports/`, `lighthouse-report.html`, `lighthouse-report.json`, and timestamped `lighthouse-report-*` files, rejects missing root, nested, and `frontend/` `.gitignore` coverage for generated Lighthouse/LHCI audit outputs, and preserves source-of-truth Lighthouse config files such as `.lighthouserc.*`.
- The Axe/Pa11y accessibility audit report guard rejects tracked local accessibility report outputs such as `axe-report/`, `axe-reports/`, `axe-report.html`, `axe-results.json`, `pa11y-report/`, `pa11y-reports/`, `pa11y-ci-report.json`, and `pa11y-results.json`, rejects missing root and nested `.gitignore` coverage for generated accessibility reports, and preserves source-of-truth accessibility configuration such as `axe.config.*`, `.pa11yci`, and `pa11yci.json`.
- The ESLint report artifact guard rejects tracked local lint report outputs such as `eslint-report/`, `eslint-reports/`, `eslint-junit.xml`, `eslint-report.json`, `eslint-report.xml`, `eslint-results.json`, and timestamped `eslint-report-*` or `eslint-results-*` files, rejects missing root and nested `.gitignore` coverage for generated ESLint reports, and preserves source-of-truth ESLint configuration such as `eslint.config.*` and `.eslintrc.*`.
- The Stylelint report artifact guard rejects tracked local lint report outputs such as `stylelint-report/`, `stylelint-reports/`, `stylelint-junit.xml`, `stylelint-report.json`, `stylelint-report.xml`, `stylelint-results.json`, and timestamped `stylelint-report-*` or `stylelint-results-*` files, rejects missing root and nested `.gitignore` coverage for generated Stylelint reports, and preserves source-of-truth Stylelint configuration such as `stylelint.config.*` and `.stylelintrc*`.
- The Biome report artifact guard rejects tracked local lint/formatter report outputs such as `biome-report/`, `biome-reports/`, `biome-report.html`, `biome-report.json`, `biome-report.xml`, `biome-results.json`, and timestamped `biome-report-*` or `biome-results-*` files, rejects missing root and nested `.gitignore` coverage for generated Biome reports, and preserves source-of-truth Biome configuration such as `biome.json` and `biome.jsonc`.
- The Markdown/prose lint report artifact guard rejects tracked local documentation lint report outputs such as `markdownlint-report/`, `markdownlint-reports/`, `markdownlint-junit.xml`, `markdownlint-report.json`, `markdownlint-results.xml`, `remark-report/`, `remark-reports/`, `remark-junit.xml`, `remark-report.json`, `remark-results.xml`, `proselint-report/`, `proselint-reports/`, `proselint-junit.xml`, `proselint-report.json`, and `proselint-results.xml`, rejects missing root and nested `.gitignore` coverage for generated Markdown/prose lint reports, and preserves source-of-truth Markdown/prose lint configuration such as `.markdownlint.*`, `.remarkrc`, `remark.config.*`, and `.proselintrc`.
- The local cache artifact guard rejects tracked local `.cache/` directories at any tree depth, and rejects missing root and nested `.gitignore` coverage for local cache artifacts.
- The local cache artifact guard rejects tracked Watchman local state cookies such as `.watchman-cookie` and `.watchman-cookie-*`, and rejects missing root and nested `.gitignore` coverage for those local Watchman state artifacts while preserving source-of-truth Watchman configuration such as `.watchmanconfig`.
- The browser-test artifact guard includes Cypress screenshot, video, and download output directories at any tree depth so local browser-run artifacts cannot be tracked as public launch evidence, with matching root and nested `.gitignore` coverage for Cypress outputs.
- The test report artifact guard rejects tracked local native compiler coverage outputs such as `*.gcda`, `*.gcno`, and `*.gcov`, and rejects missing root `.gitignore` coverage for those local coverage artifacts.
- The test report artifact guard rejects tracked LCOV HTML report directories (`lcov-report/`) at any tree depth, and rejects missing root and nested `.gitignore` coverage for those local coverage report artifacts.
- Backend acceptance artifact QA and optional MiniLM, SmolLM2, and Qwen2.5 API acceptance artifact QA self-tests run offline in default CI as schema/caveat checks only; they do not download models, start the backend, or add runtime proof.
- External OpenAI-compatible entries remain metadata placeholders; activation and `/v1/chat/completions` refuse with `external_proxy_not_implemented` and no provider call.
- PyTorch `.bin` artifacts remain blocked: the no-download public contract smoke registers a tiny synthetic local `.bin` artifact, confirms it is excluded from `/v1/models`, and confirms chat refuses with `501 not_implemented` without deserializing pickle bytes or faking inference.
- Unsupported ONNX chat/general model execution remains refused: the no-download public contract smoke registers a tiny synthetic local `.onnx` artifact, confirms it is excluded from `/v1/models`, and confirms chat refuses with `501 not_implemented` without enabling ONNX Runtime, loading the graph, or faking inference. The feature-gated MiniLM ONNX embedding path remains separate.
- Unverified SafeTensors/Hugging Face packages remain refused: the no-download public contract smoke registers a tiny synthetic local HF-style `.safetensors` package, confirms it is excluded from `/v1/models`, and confirms chat refuses with `501 not_implemented` without loading weights or faking inference. This does not broaden the verified runnable SafeTensors/HF lanes.
- GGUF metadata-only packages remain refused: the no-download public contract smoke registers a tiny synthetic local `.gguf` file, confirms it is metadata-only and excluded from `/v1/models`, and confirms chat refuses with `501 not_implemented` without making a GGUF runtime, tokenizer execution, or generation claim, runtime weight loading claim, dequantization/kernels claim, or faking inference.
- Unsupported `/v1` routes and methods remain predictable refusals: the no-download public contract smoke checks `POST /v1/responses` and `GET /v1/chat/completions` return standard JSON error envelopes for unsupported `/v1` routes and methods, without implying OpenAI Responses API support or broader method support.
- Malformed `/v1` JSON request bodies remain predictable refusals: the no-download public contract smoke checks malformed JSON on `POST /v1/chat/completions` and `POST /v1/embeddings` return `400 invalid_request` in the standard JSON error envelope, without emitting fake chat `choices` or embedding `data`.
- Default CI remains scoped to local/offline gates and does not run model downloads, networked acceptance smoke, or non-default ONNX feature tests.
- The optional Qwen2.5 0.5B Instruct catalog demo is pinned to a specific revision with exact file sizes, SHA256 hashes, Apache-2.0 metadata, local runtime-smoke evidence, and opt-in API acceptance evidence captured outside default CI; see `docs/benchmarks/2026-04-29-qwen25-local-runtime-smoke.md` and `docs/benchmarks/2026-04-29-qwen25-api-acceptance.md`.
- The optional SmolLM2 135M Instruct catalog demo has opt-in API acceptance evidence for catalog install, `/v1/models`, cold/warm non-streaming chat metrics, and streaming refusal; see `docs/benchmarks/2026-04-29-smollm2-api-acceptance.md`.
- The optional MiniLM SafeTensors embedding demo has opt-in API acceptance evidence for catalog install, `/api/embedding-models`, `/v1/models` exclusion, float `/v1/embeddings`, `encoding_format: "base64"` refused with `invalid_request`, and chat refusal; see `docs/benchmarks/2026-04-29-minilm-embeddings-api-acceptance.md`.

## What this evidence does not prove

- It does not prove broad model download success, generation quality, embedding quality, latency, throughput, production readiness, legal/license suitability, or security hardening for exposed deployments.
- It does not prove full OpenAI API parity, streaming chat, real external provider proxying, GGUF runtime/tokenizer execution/generation, ONNX chat/general execution, PyTorch `.bin` loading, or arbitrary SafeTensors/Hugging Face execution.
- Optional networked backend acceptance smoke and large-model runtime artifacts remain separate and should be reviewed before sharing artifacts publicly.
