# Public launch evidence snapshot

This snapshot records the current public launch verification state for Fathom. It is evidence for the narrow local public contract only; it is not production-readiness, performance, quality, legal/license, full OpenAI parity, streaming, external proxying, or broad model-runtime evidence.

## Snapshot

- Launch baseline commit: `a32505eadac6539865d224a8b4195656003a0032` (`Close out public launch readiness phase`)
- Latest no-download refusal evidence commit: `1b930d4ed2411fe72e66d45acc757318b649d730` (`Prove embeddings malformed JSON refusal`)
- Latest optional API evidence commit: `0655fc073af296f7687a04708053a30b9570fcdf` (`Record MiniLM embeddings API acceptance evidence`)
- Latest optional artifact QA CI wiring commit: `e9195bc7462999284960f5631d3a74aa5391bffc` (`Wire optional artifact QA into CI`)
- Latest public-contract QA hardening commit: `77caa2e610ebbd33bae8e600a6bfcdc2ffa9dfe6` (`Guard public risk scan .NET NuGet artifacts`)
- Scope: no-download public `/v1` contract, public launch checklist/evidence snapshot, manifest-driven smoke, sanitized public-contract smoke artifacts, public-contract smoke duplicate-entry schema guards, public-contract smoke Markdown/status/proof-scope row consistency, failed public-contract smoke endpoint/boundary drift guards, malformed `/v1` JSON request body refusal smoke, unsupported `/v1` route/method public-contract refusal smoke, synthetic PyTorch `.bin` public-contract refusal smoke, unsupported ONNX chat/general public-contract refusal smoke, unverified SafeTensors/Hugging Face public-contract refusal smoke, metadata-only GGUF public-contract refusal smoke, offline public-contract and backend acceptance artifact QA, backend acceptance artifact summary share-safety guard, manifest shape validation, manifest-to-`/v1` docs boundary coverage, public refusal boundary status/code/request-hint coverage, local API no-auth/loopback security-note coverage, repository-level MIT license metadata/docs consistency guard, repository text-normalization metadata guard for binary model/package/mobile/media/local-plan artifacts, API example loopback default guards, REST Client `.http` request-order/header guards and negative self-tests, tracked Python and shell syntax coverage guards, lockfile-reproducible frontend clean install guard, frontend build/UI copy launch-gate guards in the checklist and CI, CI whitespace-check wiring guard, CONTRIBUTING and PR template syntax-gate consistency checks, public security/privacy reporting copy guard, tracked-file secret-token/private-key/cloud API-key and macOS/Linux/Windows home/profile path guards, dependency lockfile local/private source guard, Git submodule metadata local/private source guard, Git LFS pointer-file guard, tracked cloud SDK credential/config guard, root `.gitignore` local cloud SDK credential/config guard, tracked Kubernetes credential/config guard, root `.gitignore` local Kubernetes credential/config guard, OS/platform metadata-file, root `.gitignore` local OS/platform metadata-file guard, editor backup/swap artifact guard, local editor history artifact guard, patch/diff artifact guard, root `.gitignore` local editor backup/swap artifact guard, root `.gitignore` local editor history artifact guard, root `.gitignore` local patch/diff artifact guard, IDE workspace/config artifact guard including `.zed/`, root `.gitignore` local IDE workspace/config artifact guard including `.zed/`, credential/config filename guard including SSH private-key filenames, `.ssh/` directories, direnv config/state, generic secret material paths, and TLS certificate/request artifacts, root `.gitignore` local credential/config guard including SSH private-key filenames, `.ssh/`, direnv config/state, generic secret material patterns, and TLS certificate/request artifact patterns, workspace/personal agent context-file guard including local assistant state such as `.cursor/` and `.windsurf/`, root `.gitignore` personal workspace context guard including local assistant state, local runtime/artifact detail-file guard including DuckDB databases/WAL files and Redis RDB snapshots, root `.gitignore` local runtime/artifact detail-file guard including DuckDB databases/WAL files and Redis RDB snapshots, local shell/REPL command history-file guard, root `.gitignore` local shell/REPL command history-file guard, local log/trace/profiling/debug-output artifact guard including SARIF/code-scanning reports and crash dumps, root `.gitignore` local log/trace/profiling/debug-output artifact guard including SARIF/code-scanning reports and crash dumps, Python cache/build artifact guard including Hypothesis example databases, root `.gitignore` local Python cache/build artifact guard, Python virtualenv/dependency artifact guard, root `.gitignore` local Python virtualenv/dependency artifact guard, frontend/Node cache/build artifact guard including package-manager caches/stores, Vite/Vitest cache/config timestamp artifacts, Storybook static build output, and lint cache files, root `.gitignore` local frontend/Node cache/build artifact guard including package-manager caches/stores, Vite/Vitest cache/config timestamp artifacts, Storybook static build output, and lint cache files, local cache artifact guard, root `.gitignore` local cache artifact guard, local temporary/scratch artifact guard, root `.gitignore` local temporary/scratch artifact guard, local test report/coverage artifact guard including Playwright/browser-test reports, Python coverage data files, and LCOV outputs, root `.gitignore` local test report/coverage artifact guard including Playwright/browser-test reports, Python coverage data files, and LCOV outputs, local notebook checkpoint artifact guard, local notebook runtime/config artifact guard, tracked notebook execution-output guard, root `.gitignore` local notebook artifact guard, Rust/Cargo cache/build artifact guard, root `.gitignore` local Rust/Cargo cache/build artifact guard, Go cache/test artifact guard, root `.gitignore` local Go cache/test artifact guard, Elixir/Mix build/dependency artifact guard, root `.gitignore` local Elixir/Mix build/dependency artifact guard, release/package artifact guard for common archive, JVM package, and installer extensions, root `.gitignore` local release/package artifact guard, backup/dump artifact guard, root `.gitignore` local backup/dump artifact guard, local model/checkpoint artifact guard, root `.gitignore` local model/checkpoint artifact guard, local ML experiment/tracking artifact guard, root `.gitignore` local ML experiment/tracking artifact guard, local Docker/container artifact guard, root `.gitignore` local Docker/container artifact guard, local deployment platform artifact guard, root `.gitignore` local deployment platform artifact guard, local Terraform/OpenTofu infrastructure state artifact guard, root `.gitignore` local infrastructure state guard, local mobile/Xcode/Android build artifact guard, root `.gitignore` local mobile/Xcode/Android build artifact guard, local screenshot/screen-recording artifact guard, root `.gitignore` local screenshot/screen-recording artifact guard, local audio/video capture/export artifact guard, root `.gitignore` local audio/video capture/export artifact guard, and symlink escape and target-resolution guards, public risk-scan caveat coverage that preserves that the scanner is not a complete privacy audit, OpenAI SDK example offline request/response regression, API contract issue-template privacy checks, required public issue-template field/privacy checkbox guard, blank public issue disablement checks, bug/security issue-template privacy checks, PR template truthfulness/privacy checks, CI token-permissions guard, CI public risk-scan wiring guard, public overclaim scanner self-test coverage, production-readiness/legal-license public overclaim examples, offline MiniLM/SmolLM2/Qwen2.5 optional API acceptance artifact QA self-tests, CI/static policy wiring, and Phase 16 narrow catalog-backed optional API/runtime evidence for MiniLM embeddings, SmolLM2 135M Instruct, and Qwen2.5 0.5B Instruct.
- Additional current public risk-scan scope: R/RStudio local session/dependency artifact guard and matching root `.gitignore` local R/RStudio artifact coverage.
- Additional current public risk-scan scope: Julia depot/preference artifact guard for `.julia/`, `LocalPreferences.toml`, `*.jl.cov`, and `*.jl.mem`, with matching root `.gitignore` coverage and source-file allowances preserving source-of-truth Julia project files such as `Project.toml`, `Manifest.toml`, `Artifacts.toml`, and Julia source files.
- Additional current public risk-scan scope: Gradle/JVM local build artifact guard for `.gradle/` cache/state and common Gradle `build/` output subtrees, with matching root `.gitignore` coverage and project-file allowances for `build.gradle`, `settings.gradle`, `gradle.properties`, `gradlew`, and `gradle/wrapper/gradle-wrapper.properties`.
- Additional current public risk-scan scope: PHP Composer local dependency/test artifact guard and matching root `.gitignore` local PHP Composer artifact coverage.
- Additional current public risk-scan scope: Bazel local build output guard for `bazel-bin/`, `bazel-out/`, `bazel-testlogs/`, and root `bazel-*` outputs, with matching root `.gitignore` coverage and source-file allowances for `BUILD`, `BUILD.bazel`, `MODULE.bazel`, and `.bzl` files.
- Additional current public risk-scan scope: ML experiment/tracking artifact guard for W&B, MLflow, Lightning, and TensorBoard run outputs, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: Elixir/Mix local build/dependency artifact guard for `.elixir_ls/`, `_build/`, and `deps/`, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: Dart/Flutter local tool/cache artifact guard for `.dart_tool/`, `.pub-cache/`, `.pub/`, `.packages`, `.flutter-plugins`, and `.flutter-plugins-dependencies`, with matching root `.gitignore` coverage and source-file allowances for `pubspec.yaml`, `pubspec.lock`, Dart source, Android Gradle project files, and iOS Xcode project files.
- Additional current public risk-scan scope: Expo local project state guard for `.expo/` and `.expo-shared/`, with matching root `.gitignore` coverage and source-file allowances for mobile app source, Expo config, Android Gradle project files, and iOS Xcode project files.
- Additional current public risk-scan scope: Android native build intermediate guard for `.cxx/` and `.externalNativeBuild/`, including the same directories under `android/`, with matching root `.gitignore` coverage and source-file allowances for Android Gradle project files, JNI/C++ source files, and checked-in native build configuration.
- Additional current public risk-scan scope: CocoaPods dependency output guard for root `Pods/` and `ios/Pods/`, with matching root `.gitignore` coverage and source-file allowances for `Podfile` and `Podfile.lock`.
- Additional current public risk-scan scope: Carthage dependency output guard for `Carthage/Build/` and `Carthage/Checkouts/`, with matching root `.gitignore` coverage and source-file allowances for `Cartfile` and `Cartfile.resolved`.
- Additional current public risk-scan scope: PDM/PEP 582 local Python dependency/build artifact guard for `.pdm-build/` and `__pypackages__/`, with matching root `.gitignore` coverage.
- Additional current public risk-scan scope: local Kotlin/Kotlin Native compiler artifact guard for `.kotlin/` and `.konan/`, with matching root `.gitignore` coverage and source-file allowances for `build.gradle.kts`, `settings.gradle.kts`, Kotlin source files, and Gradle version catalog files.
- Additional current public risk-scan scope: Clojure/Leiningen local artifact guard for `.lein/`, `.cpcache/`, `.shadow-cljs/`, and `.nrepl-port`, with matching root `.gitignore` coverage and source-file allowances for `project.clj`, `deps.edn`, `bb.edn`, `shadow-cljs.edn`, and Clojure/ClojureScript source files.
- Additional current public risk-scan scope: Scala/SBT local build server/IDE artifact guard for `.bloop/`, `.bsp/`, `.metals/`, and `.scala-build/`, with matching root `.gitignore` coverage and source-file allowances for `build.sbt`, `project/build.properties`, and Scala source files.
- Additional current public risk-scan scope: Haskell Stack/Cabal local build artifact guard for `.stack-work/`, `dist-newstyle/`, `.cabal-sandbox/`, and `cabal.sandbox.config`, with matching root `.gitignore` coverage and source-file allowances for `.cabal` files, `stack.yaml`, `cabal.project`, and Haskell source files.
- Additional current public risk-scan scope: Zig local build artifact guard for `.zig-cache/`, `zig-cache/`, and `zig-out/`, with matching root `.gitignore` coverage and source-file allowances for `build.zig`, `build.zig.zon`, and Zig source files.
- Additional current public risk-scan scope: local OCaml/opam artifact guard for `_opam/` and `.opam-switch/`, with matching root `.gitignore` coverage and source-file allowances for `dune`, `dune-project`, `*.opam`, and OCaml source files.
- Additional current public risk-scan scope: Lua/LuaRocks local dependency/build/package artifact guard for `.luarocks/`, `lua_modules/`, and `*.rock`, with matching root `.gitignore` coverage and source-file allowances for `*.lua`, `*.rockspec`, LuaRocks lock files, and Lua config files.
- Additional current public risk-scan scope: Erlang/Rebar3 local cache/crash artifact guard for `.rebar3/`, `rebar3.crashdump`, and `erl_crash.dump`, with matching root `.gitignore` coverage and source-file allowances for `rebar.config`, `rebar.lock`, Erlang source files, and Erlang header files.
- Additional current public risk-scan scope: JVM compiler bytecode artifact guard for `*.class` and `*.tasty`, with matching root `.gitignore` coverage and source-file allowances for JVM project files and Java/Kotlin/Scala source files.
- Additional current public risk-scan scope: .NET/NuGet local dependency/build/user-state artifact guard for `.nuget/`, `packages/`, `bin/Debug/`, `obj/Release/`, `project.assets.json`, `project.nuget.cache`, `*.nupkg`, `*.snupkg`, `*.csproj.user`, and `*.suo`, with matching root `.gitignore` coverage and source-file allowances for .NET project files, solution files, source files, and NuGet lock files.
- Additional current public risk-scan scope: Deno local cache artifact guard for `.deno/` and `deno-dir/`, with matching root `.gitignore` coverage and source-file allowances for `deno.json`, `deno.jsonc`, `deno.lock`, JavaScript files, and TypeScript files.
- Fresh-clone QA: passed for the launch baseline and earlier evidence commits. For `128550a`, focused fresh-clone QA passed after reducing cold-build pressure with `CARGO_BUILD_JOBS=1` and shared local target artifacts. Later optional API evidence, optional artifact-QA CI wiring, no-download refusal evidence, and public-contract QA hardening commits are covered by focused static/public-risk/artifact QA gates and preserved opt-in local artifact QA, not a new full fresh-clone run.
- Public risk-scan hardening detail: PID/process marker files (`*.pid`), DuckDB databases/WAL files (`*.duckdb`, `*.duckdb.wal`), Redis RDB snapshots (`*.rdb`, `dump.rdb`), patch/diff files (`*.patch`, `*.diff`), local editor history directories (`.history/`), Python Hypothesis example databases (`.hypothesis/`), Python coverage data files (`.coverage`, `.coverage.*`), NYC/Istanbul coverage artifacts such as `.nyc_output/`, `coverage-final.json`, and `clover.xml`, local Python package-manager/dependency artifacts (`.pdm-build/`, `__pypackages__/`, `.uv/`, `.uv-cache/`, `.pip-cache/`, `uv-cache/`, `pip-cache/`), Ruby/Bundler local dependency artifacts (`.bundle/`, `vendor/bundle/`, `vendor/cache/`), R/RStudio local session/dependency artifacts (`.Rproj.user/`, `.Rhistory`, `.RData`, `.Ruserdata`, `renv/library/`), Julia depot/preference artifacts (`.julia/`, `LocalPreferences.toml`, `*.jl.cov`, `*.jl.mem`), Go cache/test generated outputs (`.gocache/`, `.gomodcache/`, `coverage.out`, `*.test`), Elixir/Mix build/dependency outputs (`.elixir_ls/`, `_build/`, `deps/`), Erlang/Rebar3 cache/crash outputs (`.rebar3/`, `rebar3.crashdump`, `erl_crash.dump`), JVM compiler bytecode outputs (`*.class`, `*.tasty`), .NET/NuGet dependency/build/user-state artifacts (`.nuget/`, `packages/`, `bin/Debug/`, `obj/Release/`, `project.assets.json`, `project.nuget.cache`, `*.nupkg`, `*.snupkg`, `*.csproj.user`, `*.suo`), JVM package archives (`*.jar`, `*.war`, `*.ear`), Haskell Stack/Cabal build outputs (`.stack-work/`, `dist-newstyle/`, `.cabal-sandbox/`, `cabal.sandbox.config`), OCaml/opam local switch artifacts (`_opam/`, `.opam-switch/`), Lua/LuaRocks local dependency/build/package outputs (`.luarocks/`, `lua_modules/`, `*.rock`), Zig build outputs (`.zig-cache/`, `zig-cache/`, `zig-out/`), ML experiment/tracking outputs (`.wandb/`, `wandb/`, `mlruns/`, `lightning_logs/`, `events.out.tfevents.*`), LCOV coverage outputs (`lcov.info`, `*.lcov`), SARIF/code-scanning reports (`*.sarif`, `*.sarif.json`), Windows Explorer thumbnail caches (`ehthumbs.db`) and recycle-bin metadata (`$RECYCLE.BIN/`), JetBrains/Eclipse/Zed IDE artifacts (`*.iml`, `.classpath`, `.project`, `.settings/`, `.zed/`), TLS certificate/request artifacts (`*.crt`, `*.csr`, `*.der`, `*.p7b`, `*.p7c`), app-signing key stores (`*.p8`, `*.jks`, `*.keystore`), Expo local state (`.expo/`, `.expo-shared/`), Android native build intermediates (`.cxx/`, `.externalNativeBuild/`), CocoaPods dependency outputs (`Pods/`, `ios/Pods/`), Carthage dependency outputs (`Carthage/Build/`, `Carthage/Checkouts/`), mobile provisioning/build archives (`*.mobileprovision`, `*.provisionprofile`, `*.xcarchive`), Swift Package Manager build/workspace artifacts (`.build/`, `.swiftpm/`), Bazel build outputs (`bazel-bin/`, `bazel-out/`, `bazel-testlogs/`, root `bazel-*`), Playwright/browser-test report directories (`playwright-report/`, `blob-report/`, `.playwright/`), Cypress browser-test screenshot/video/download directories (`cypress/screenshots/`, `cypress/videos/`, `cypress/downloads/`), frontend lint cache files (`.eslintcache`, `.stylelintcache`), Bun cache/debug-log artifacts (`.bun/`, `bun-debug.log`), local cache directories (`.cache/`), temporary scratch directories (`tmp/`, `temp/`) and files (`*.tmp`, `*.temp`), Jupyter runtime/config artifacts (`.jupyter/`, `.nbhistory`), Terraform/OpenTofu state artifacts (`.terraform/`, `.tfstate`, `.tfvars`, `.tfplan`), deployment platform local state directories (`.vercel/`, `.netlify/`), native/CMake build artifacts, default screenshot/screen-recording captures, and audio/video capture exports are blocked as tracked local artifacts, with matching root `.gitignore` coverage.
- The local documentation build artifact guard rejects tracked LaTeX/Pandoc-adjacent build byproducts such as `*.aux`, `*.bbl`, `*.blg`, `*.fdb_latexmk`, `*.fls`, `*.synctex.gz`, and `_minted-*` caches, and rejects missing root `.gitignore` coverage for local documentation build artifacts. It avoids a blanket PDF block so source-of-truth public documents are not rejected solely by extension.
- The local ML experiment/tracking artifact guard rejects tracked W&B, MLflow, Lightning, and TensorBoard run outputs such as `.wandb/`, `wandb/`, `mlruns/`, `lightning_logs/`, and `events.out.tfevents.*`, and rejects missing root `.gitignore` coverage for local ML experiment/tracking artifacts.
- The local Meson build artifact guard rejects tracked local Meson build artifacts such as `.mesonpy-*`, `meson-info/`, `meson-logs/`, and `meson-private/`, and rejects missing root `.gitignore` coverage for local Meson build artifacts while preserving source-of-truth Meson files such as `meson.build`, `meson_options.txt`, and `meson.options`.
- The local Nix build result artifact guard rejects tracked local Nix build result artifacts such as root `result` and `result-*` outputs, and rejects missing root `.gitignore` coverage for local Nix build result artifacts.
- The local Bazel build artifact guard rejects tracked local Bazel build artifacts such as `bazel-bin/`, `bazel-out/`, `bazel-testlogs/`, and root `bazel-*` outputs, and rejects missing root `.gitignore` coverage for local Bazel build artifacts while preserving source-of-truth Bazel files such as `BUILD`, `BUILD.bazel`, `MODULE.bazel`, and `.bzl` files.
- The local Swift Package Manager artifact guard rejects tracked local Swift Package Manager build/workspace artifacts such as `.build/` and `.swiftpm/`, and rejects missing root `.gitignore` coverage for local Swift Package Manager artifacts.
- The local Zig build artifact guard rejects tracked local Zig build artifacts such as `.zig-cache/`, `zig-cache/`, and `zig-out/`, and rejects missing root `.gitignore` coverage for local Zig build artifacts while preserving source-of-truth Zig files such as `build.zig`, `build.zig.zon`, and Zig source files.
- The local Dart/Flutter artifact guard rejects tracked local Dart/Flutter artifacts such as `.dart_tool/`, `.pub-cache/`, `.pub/`, `.packages`, `.flutter-plugins`, and `.flutter-plugins-dependencies`, and rejects missing root `.gitignore` coverage for local Dart/Flutter artifacts while preserving source-of-truth project files such as `pubspec.yaml`, `pubspec.lock`, Dart source, Android Gradle project files, and iOS Xcode project files.
- The local native/CMake build artifact guard rejects tracked local native/CMake build artifacts such as `cmake-build-*`, `CMakeFiles/`, `CMakeCache.txt`, `cmake_install.cmake`, and `compile_commands.json`, and rejects missing root `.gitignore` coverage for local native/CMake build artifacts.
- Local CI runner hardening detail: local `act` workflow runner state/config (`.act/` and `.actrc`) is blocked as tracked launch-facing material, with matching root `.gitignore` coverage.
- The Ruby/Bundler dependency artifact guard rejects tracked local Bundler state/cache paths such as `.bundle/`, `vendor/bundle/`, and `vendor/cache/`, and rejects missing root `.gitignore` coverage for local Ruby/Bundler dependency artifacts.
- The PHP Composer dependency/test artifact guard rejects tracked local Composer/PHPUnit artifacts such as `vendor/autoload.php`, `vendor/bin/`, `vendor/composer/`, `.phpunit.cache/`, `.phpunit.result.cache`, and `composer.phar`, and rejects missing root `.gitignore` coverage for local PHP Composer dependency/test artifacts.
- The R/RStudio artifact guard rejects tracked local R/RStudio artifacts such as `.Rproj.user/`, `.Rhistory`, `.RData`, `.Ruserdata`, and `renv/library/`, and rejects missing root `.gitignore` coverage for local R/RStudio artifacts.
- The Julia depot/preference artifact guard rejects tracked local Julia depot/preference artifacts such as `.julia/`, `LocalPreferences.toml`, `*.jl.cov`, and `*.jl.mem`, and rejects missing root `.gitignore` coverage for local Julia depot/preference artifacts while preserving source-of-truth Julia project files such as `Project.toml`, `Manifest.toml`, `Artifacts.toml`, and Julia source files.
- The Go cache/test artifact guard rejects tracked local Go cache/test artifacts such as `.gocache/`, `.gomodcache/`, `coverage.out`, and `*.test`, and rejects missing root `.gitignore` coverage for local Go cache/test artifacts.
- The Elixir/Mix build/dependency artifact guard rejects tracked local Elixir/Mix build/dependency artifacts such as `.elixir_ls/`, `_build/`, and `deps/`, and rejects missing root `.gitignore` coverage for local Elixir/Mix build/dependency artifacts.
- The Erlang/Rebar3 artifact guard rejects tracked local cache and crash artifacts such as `.rebar3/`, `rebar3.crashdump`, and `erl_crash.dump`, and rejects missing root `.gitignore` coverage for local Erlang/Rebar3 artifacts while preserving source-of-truth Erlang/Rebar3 files such as `rebar.config`, `rebar.lock`, Erlang source files, and Erlang header files.
- The JVM dependency artifact guard rejects tracked local JVM dependency artifacts such as `.m2/`, and rejects missing root `.gitignore` coverage for local JVM dependency artifacts.
- The JVM compiler artifact guard rejects tracked local bytecode outputs such as `*.class` and `*.tasty`, and rejects missing root `.gitignore` coverage for local JVM compiler artifacts while preserving source-of-truth JVM project files such as `pom.xml`, `build.gradle`, `build.sbt`, Java source files, Kotlin source files, and Scala source files.
- The .NET/NuGet artifact guard rejects tracked local dependency/build/user-state artifacts such as `.nuget/`, `packages/`, `bin/Debug/`, `obj/Release/`, `project.assets.json`, `project.nuget.cache`, `*.nupkg`, `*.snupkg`, `*.csproj.user`, and `*.suo`, and rejects missing root `.gitignore` coverage for local .NET/NuGet artifacts while preserving source-of-truth .NET project files such as `*.csproj`, `*.fsproj`, `*.vbproj`, `*.sln`, `Directory.Build.props`, C# source files, F# source files, and NuGet lock files.
- The Clojure/Leiningen artifact guard rejects tracked local artifacts such as `.lein/`, `.cpcache/`, `.shadow-cljs/`, and `.nrepl-port`, and rejects missing root `.gitignore` coverage for local Clojure/Leiningen artifacts while preserving source-of-truth Clojure files such as `project.clj`, `deps.edn`, `bb.edn`, `shadow-cljs.edn`, and Clojure/ClojureScript source files.
- The Gradle/JVM build artifact guard rejects tracked local Gradle cache/state and build outputs such as `.gradle/`, `app/build/classes/`, `service/build/reports/`, `service/build/test-results/`, `service/build/tmp/`, `service/build/generated/`, and `service/build/libs/`, and rejects missing root `.gitignore` coverage for local Gradle/JVM build artifacts.
- The local Kotlin/Kotlin Native compiler artifact guard rejects tracked local Kotlin/Kotlin Native compiler artifacts such as `.kotlin/` and `.konan/`, and rejects missing root `.gitignore` coverage for local Kotlin/Kotlin Native artifacts while preserving source-of-truth Kotlin files such as `build.gradle.kts`, `settings.gradle.kts`, Kotlin source files, and Gradle version catalog files.
- The Scala/SBT build artifact guard rejects tracked local Scala build server/IDE outputs such as `.bloop/`, `.bsp/`, `.metals/`, and `.scala-build/`, and rejects missing root `.gitignore` coverage for local Scala/SBT build artifacts while preserving source-of-truth Scala/SBT files such as `build.sbt`, `project/build.properties`, and Scala source files.
- The Haskell Stack/Cabal build artifact guard rejects tracked local build outputs such as `.stack-work/`, `dist-newstyle/`, `.cabal-sandbox/`, and `cabal.sandbox.config`, and rejects missing root `.gitignore` coverage for local Haskell Stack/Cabal build artifacts while preserving source-of-truth Haskell files such as `.cabal` files, `stack.yaml`, `cabal.project`, and Haskell source files.
- The local OCaml/opam artifact guard rejects tracked local OCaml/opam local switch artifacts such as `_opam/` and `.opam-switch/`, and rejects missing root `.gitignore` coverage for local OCaml/opam artifacts while preserving source-of-truth OCaml project files such as `dune`, `dune-project`, `*.opam`, and OCaml source files.
- The Lua/LuaRocks artifact guard rejects tracked local Lua/LuaRocks dependency/build/package artifacts such as `.luarocks/`, `lua_modules/`, and `*.rock`, and rejects missing root `.gitignore` coverage for local Lua/LuaRocks artifacts while preserving source-of-truth Lua files such as `*.lua`, `*.rockspec`, LuaRocks lock files, and Lua config files.
- The Python cache/build artifact guard rejects tracked local Pyre/Pytype and mypy daemon state such as `.pyre/`, `.pytype/`, and `.dmypy.json`, plus Python package metadata/build artifacts such as `.eggs/`, `*.egg-info/`, and `*.dist-info/`, and rejects missing root `.gitignore` coverage for local Python cache/build artifacts. The Python virtualenv/dependency artifact guard also rejects local PDM/PEP 582 artifacts such as `.pdm-build/` and `__pypackages__/`, and rejects missing root `.gitignore` coverage for those local Python dependency/build artifacts.
- Dependency lockfile source hardening includes Yarn `portal:` dependencies and local Yarn `patch:` references so those cannot quietly smuggle local/private source paths into public launch evidence.

## Gates represented by the snapshot

The referenced fresh-clone QA gates verified:

- exact `origin/main` checkout and clean initial/final status;
- no repo-local `AGENTS.md`;
- no tracked files larger than 1 MiB;
- public risk scan and focused sensitive/overclaim review;
- JSON, Python, shell, static QA, API-example regression, and artifact-QA gates;
- `cargo fmt --all --check` and `cargo test -q`;
- manifest-driven public contract smoke in default and artifact modes;
- public-contract smoke artifact summaries accepted by `scripts/public_contract_smoke_artifact_qa.py`;
- manifest shape validation and manifest-to-`/v1` docs boundary coverage in `scripts/public_api_contract_qa.py`;
- MiniLM, SmolLM2, and Qwen2.5 optional API acceptance artifact schemas covered by offline self-tests in default CI;
- lockfile-reproducible frontend clean install plus frontend build/copy QA.

## What this evidence proves

- The documented no-download public `/v1` routing/refusal contract works against the real backend process in isolated empty state.
- Public-contract smoke summary artifacts have an offline QA gate for schema, pass/fail semantics, coverage, caveats, and share-safety.
- Public-contract smoke summary artifacts reject duplicate endpoint, boundary, and deferred-boundary entries so summary coverage cannot be inflated by repeated rows.
- Public-contract smoke summary artifacts keep Markdown endpoint/boundary/deferred/status/proof-scope rows aligned with JSON evidence rows.
- The backend acceptance artifact summary share-safety guard rejects local paths, secret markers, and request/payload text in shareable `summary.json` and `summary.md` evidence before those summaries are treated as public handoff artifacts.
- The offline public API contract QA gate validates manifest shape, keeps manifest boundary names covered by the `/v1` docs and refusal matrix, and requires request hints for status/code refusal boundaries to be exposed in the refusal matrix.
- The offline public API contract QA gate keeps the local API no-auth, loopback-only, internet/untrusted-LAN exposure, and `SECURITY.md` review warnings attached to the README, backend quickstart, `/v1` contract, and public launch checklist.
- The offline public API contract QA gate keeps repository-level MIT license metadata aligned across Cargo manifests, README, and `LICENSE`.
- The offline public API contract QA gate keeps root `.gitattributes` text-normalization metadata present, including binary classifications for common model, package, mobile, media, and local plan artifacts, so public diffs remain stable across platforms without line-ending normalization of binary evidence.
- The offline public API contract QA gate keeps every tracked Python helper covered by the documented and CI `python3 -m py_compile` syntax gate and every tracked shell helper covered by the documented and CI `bash -n` syntax gate, including local oracle helpers that are not run during no-download launch QA.
- The offline public API contract QA gate keeps the public launch clean-clone frontend install on `npm --prefix frontend ci`, so release handoffs use the checked-in lockfile rather than refreshing dependency resolution with `npm install`.
- The offline public API contract QA gate keeps CONTRIBUTING and the pull request template aligned with the launch checklist on Python and shell syntax gates, so contributors and reviewers do not lose the syntax checks when using common gates or PR checkboxes.
- The offline API client example regression gate runs the OpenAI SDK example through a dependency-free local `openai` stub and checks both request routing/body shape and the JSON response payloads printed by the example, including opt-in float embeddings.
- The offline API client example regression gate keeps the REST Client `.http` example on the expected request sequence and requires JSON `Content-Type` headers on every documented POST block, with synthetic negative self-tests for missing headers, request-order drift, streaming chat, and non-float encoding drift.
- The public API contract QA self-test exercises synthetic refused/unsupported public overclaim examples for streaming, external proxying, OpenAI parity, SafeTensors blanket execution, base64 embeddings, production readiness, and legal/license suitability, plus allowed caveated examples.
- The offline public API contract QA gate keeps public issue-template intake fields and privacy/safety checkbox options required, so API contract, model/runtime, bug, and security/privacy reports cannot silently become optional public artifact-disclosure prompts.
- The offline public API contract QA and CI static policy gates require default CI to run both the public risk scan self-test and the tracked-file public risk scan, while preserving the public caveat that the scan is not a complete privacy audit.
- The public risk scan rejects obvious tracked secret-token values such as OpenAI-style, GitHub, Hugging Face, Slack, AWS access key IDs, Google API keys, and bearer tokens while allowing placeholder API-key copy. It also rejects PEM private-key block headers while allowing public-key headers, macOS, Linux, and Windows home/profile paths, dependency lockfile local file/path sources, SSH-only dependency sources, authenticated dependency URLs, secret values, private local paths, tracked Git submodule metadata such as `.gitmodules` local/relative, SSH-only, authenticated, secret, or private source URLs, tracked Git LFS pointer files that can hide external artifact downloads, tracked cloud SDK credential/config files such as `.aws/`, `.azure/`, `.config/gcloud/`, `.boto`, `boto.cfg`, `application_default_credentials.json`, `service-account.json`, `service_account.json`, and `serviceAccountKey.json`, missing root `.gitignore` coverage for local cloud SDK credential/config files, tracked Kubernetes credential/config files such as `.kube/`, `kubeconfig`, `kubeconfig.yaml`, and `kubeconfig.yml`, missing root `.gitignore` coverage for local Kubernetes credential/config files, tracked OS/platform metadata files such as `.DS_Store`, `Thumbs.db`, `ehthumbs.db`, `desktop.ini`, `$RECYCLE.BIN/`, `__MACOSX/`, `._*`, `.AppleDouble`, `.fseventsd/`, `.Spotlight-V100/`, `.TemporaryItems/`, `.Trashes/`, `.LSOverride`, and `.localized`, missing root `.gitignore` coverage for local OS/platform metadata files, tracked editor backup/swap artifacts, tracked local editor history artifacts such as `.history/`, tracked patch/diff artifacts, missing root `.gitignore` coverage for local editor backup/swap files, missing root `.gitignore` coverage for local editor history directories, missing root `.gitignore` coverage for local patch/diff files, tracked IDE workspace/config artifacts, missing root `.gitignore` coverage for local IDE workspace/config artifacts, tracked credential/config filenames including `.envrc`, SSH private-key filenames such as `id_rsa`, `id_ed25519`, `id_ecdsa`, and `id_dsa`, tracked `.direnv/`, `.ssh/`, `secrets/`, and `private/` directories, tracked generic secret material files such as `secrets.json`, `secrets.yaml`, `secrets.yml`, `*.secret`, and `*.secrets`, plus TLS certificate/request artifacts such as `*.crt`, `*.csr`, `*.der`, `*.p7b`, and `*.p7c`, missing root `.gitignore` coverage for local credential/config files including `/.ssh/`, SSH private-key filenames, `/.direnv/`, `.envrc`, `/secrets/`, `/private/`, and generic secret material patterns and TLS certificate/request artifact patterns, tracked workspace/personal agent context files including local assistant state such as `.codex/`, `.claude/`, `.continue/`, `.cursor/`, `.windsurf/`, and `.aider.*`, missing root `.gitignore` coverage for personal workspace context including local assistant state, tracked local shell/REPL command history files such as `.bash_history`, `.zsh_history`, `.python_history`, `.node_repl_history`, `.psql_history`, `.sqlite_history`, `.mysql_history`, `.rediscli_history`, and `.fish_history`, missing root `.gitignore` coverage for local shell/REPL command history files, tracked local runtime/artifact detail files, missing root `.gitignore` coverage for local runtime/artifact detail files, tracked local log/trace/profiling/debug-output artifacts such as top-level `logs/`, `traces/`, `profiles/`, `debug-output/`, `.trace`, `.cpuprofile`, `.heapsnapshot`, `.perf`, `.prof`, `.sarif`, `.sarif.json`, `.core`, `.crash`, `.dmp`, `.ips`, and root `core` files, missing root `.gitignore` coverage for local log/trace/profiling/debug-output artifacts including `*.log`, SARIF/code-scanning reports, and crash dump patterns, tracked Python cache/build artifacts such as `.hypothesis/`, `__pycache__/`, `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, and `.pyc`/`.pyo` files, missing root `.gitignore` coverage for local Python cache/build artifacts, tracked Python virtualenv/dependency artifacts such as `.venv/`, `venv/`, `env/`, `.tox/`, `.nox/`, `wheelhouse/`, `pip-wheel-metadata/`, and `site-packages/`, missing root `.gitignore` coverage for local Python virtualenv/dependency artifacts, tracked frontend/Node cache/build artifacts including root-level `dist/` or `build/` output, Bun package-manager caches/logs, Vite/Vitest cache/config timestamp artifacts, and lint cache files such as `.eslintcache` and `.stylelintcache`, missing root `.gitignore` coverage for local frontend/Node cache/build artifacts, tracked local cache artifacts such as `.cache/`, missing root `.gitignore` coverage for local cache artifacts, tracked local temporary/scratch artifacts such as `tmp/`, `temp/`, `*.tmp`, and `*.temp`, missing root `.gitignore` coverage for local temporary/scratch artifacts, tracked local test report/coverage artifacts such as `test-results/`, `test-reports/`, `reports/`, `htmlcov/`, `.coverage`, `.coverage.*`, `coverage.xml`, `lcov.info`, `*.lcov`, `junit.xml`, and `*.junit.xml`, missing root `.gitignore` coverage for local test report/coverage artifacts, Playwright/browser-test report directories such as `playwright-report/`, `blob-report/`, and `.playwright/`, tracked local notebook checkpoint artifacts such as `.ipynb_checkpoints/`, tracked local notebook runtime/config artifacts such as `.jupyter/` and `.nbhistory`, tracked notebook execution outputs, missing root `.gitignore` coverage for local notebook artifacts, tracked Rust/Cargo cache/build artifacts, missing root `.gitignore` coverage for local Rust/Cargo cache/build artifacts, tracked local native/CMake build artifacts such as `cmake-build-*`, `CMakeFiles/`, `CMakeCache.txt`, `cmake_install.cmake`, and `compile_commands.json`, missing root `.gitignore` coverage for local native/CMake build artifacts, tracked release/package artifacts such as `.deb`, `.rpm`, `.msi`, `.rar`, `.zst`, `.jar`, `.war`, and `.ear`, missing root `.gitignore` coverage for local release/package artifacts, tracked backup/dump artifacts, missing root `.gitignore` coverage for local backup/dump artifacts, tracked local model/checkpoint artifacts such as top-level `models/`, `model-store/`, `weights/`, `checkpoints/`, `.safetensors`, `.gguf`, `.onnx`, `.bin`, `.pt`, `.pth`, `.ckpt`, `.npz`, `.npy`, and `.tflite` files, missing root `.gitignore` coverage for local model/checkpoint artifacts, tracked local Docker/container artifacts such as top-level `.docker/`, `docker-data/`, `docker-volumes/`, `docker-compose.override.yml`, `docker-compose.override.yaml`, `compose.override.yml`, and `compose.override.yaml`, missing root `.gitignore` coverage for local Docker/container artifacts, tracked local deployment platform artifacts such as `.vercel/` and `.netlify/`, missing root `.gitignore` coverage for local deployment platform artifacts, tracked local Terraform/OpenTofu infrastructure state artifacts such as `.terraform/`, `.terraform.lock.hcl`, `.tfstate`, `.tfvars`, `.tfvars.json`, and `.tfplan` files, missing root `.gitignore` coverage for local infrastructure state artifacts, tracked local mobile/Xcode/Android build artifacts such as `DerivedData/`, `.gradle/`, `xcuserdata/`, `local.properties`, `.xcuserstate`, `.xcresult`, `.ipa`, `.apk`, `.aab`, and `.dSYM` files, missing root `.gitignore` coverage for local mobile/Xcode/Android build artifacts, tracked local screenshot/screen-recording artifacts with default macOS capture prefixes, missing root `.gitignore` coverage for local screenshot/screen-recording artifacts, tracked local audio/video capture/export artifacts with common media extensions and default voice memo/audio recording prefixes, missing root `.gitignore` coverage for local audio/video capture/export artifacts, tracked symlinks that escape the repository or resolve only to missing/untracked local targets, and tracked files larger than 1 MiB so launch-facing checks catch accidental machine-local noise or large artifacts before publication.
- The Rust/Cargo guard rejects tracked Rust/Cargo cache/build artifacts such as `.cargo/`, `target/`, compiler outputs, and coverage profiles, with matching root `.gitignore` coverage.
- The ML experiment/tracking artifact guard rejects tracked W&B, MLflow, Lightning, and TensorBoard run outputs such as `.wandb/`, `wandb/`, `mlruns/`, `lightning_logs/`, and `events.out.tfevents.*`, with matching root `.gitignore` coverage.
- The frontend/Node cache guard includes package-manager caches/stores such as `.bun/`, `.npm/`, `.pnpm-store/`, `.yarn/cache/`, `.yarn/unplugged/`, `.yarn/build-state.yml`, and `.yarn/install-state.gz`, plus Bun debug logs, Vite/Vitest cache/config timestamp artifacts, Storybook static build output, Astro/Docusaurus/Hugo/Jekyll/VitePress/Next.js/SvelteKit/Nuxt/Angular framework cache/build outputs such as `.astro/`, `.docusaurus/`, `resources/_gen/`, `.hugo_build.lock`, `_site/`, `.jekyll-cache/`, `.sass-cache/`, `.vitepress/cache/`, `.vitepress/dist/`, `.next/`, `.svelte-kit/`, `.nuxt/`, `.output/`, and `.angular/cache/`, and lint cache files such as `.eslintcache` and `.stylelintcache`, with matching root `.gitignore` coverage.
- The Deno local cache artifact guard rejects tracked local Deno cache artifacts such as `.deno/` and `deno-dir/`, and rejects missing root `.gitignore` coverage for local Deno cache artifacts while preserving source-of-truth Deno project files such as `deno.json`, `deno.jsonc`, `deno.lock`, JavaScript files, and TypeScript files.
- The browser-test artifact guard includes Cypress screenshot, video, and download output directories so local browser-run artifacts cannot be tracked as public launch evidence.
- Backend acceptance artifact QA and optional MiniLM, SmolLM2, and Qwen2.5 API acceptance artifact QA self-tests run offline in default CI as schema/caveat checks only; they do not download models, start the backend, or add runtime proof.
- External OpenAI-compatible entries remain metadata placeholders; activation and `/v1/chat/completions` refuse with `external_proxy_not_implemented` and no provider call.
- PyTorch `.bin` artifacts remain blocked: the no-download public contract smoke registers a tiny synthetic local `.bin` artifact, confirms it is excluded from `/v1/models`, and confirms chat refuses with `501 not_implemented` without deserializing pickle bytes or faking inference.
- Unsupported ONNX chat/general model execution remains refused: the no-download public contract smoke registers a tiny synthetic local `.onnx` artifact, confirms it is excluded from `/v1/models`, and confirms chat refuses with `501 not_implemented` without enabling ONNX Runtime, loading the graph, or faking inference. The feature-gated MiniLM ONNX embedding path remains separate.
- Unverified SafeTensors/Hugging Face packages remain refused: the no-download public contract smoke registers a tiny synthetic local HF-style `.safetensors` package, confirms it is excluded from `/v1/models`, and confirms chat refuses with `501 not_implemented` without loading weights or faking inference. This does not broaden the verified runnable SafeTensors/HF lanes.
- GGUF metadata-only packages remain refused: the no-download public contract smoke registers a tiny synthetic local `.gguf` file, confirms it is metadata-only and excluded from `/v1/models`, and confirms chat refuses with `501 not_implemented` without making a GGUF runtime, tokenizer execution, or generation claim, runtime weight loading claim, dequantization/kernels claim, or faking inference.
- Unsupported `/v1` routes and methods remain predictable refusals: the no-download public contract smoke checks `POST /v1/responses` and `GET /v1/chat/completions` return standard JSON error envelopes for unsupported `/v1` routes and methods, without implying OpenAI Responses API support or broader method support.
- Malformed `/v1` JSON request bodies remain predictable refusals: the no-download public contract smoke checks malformed JSON on `POST /v1/chat/completions` and `POST /v1/embeddings` return `400 invalid_request` in the standard JSON error envelope, without emitting fake chat `choices` or embedding `data`.
- Default CI remains scoped to local/offline gates and does not run model downloads, networked acceptance smoke, or non-default ONNX feature tests.
- The optional Qwen2.5 0.5B Instruct catalog demo is pinned to a specific revision with exact file sizes, SHA256 hashes, Apache-2.0 metadata, local runtime-smoke evidence, and opt-in API acceptance evidence captured outside default CI; see `docs/benchmarks/2026-04-29-qwen25-local-runtime-smoke.md` and `docs/benchmarks/2026-04-29-qwen25-api-acceptance.md`.
- The optional SmolLM2 135M Instruct catalog demo has opt-in API acceptance evidence for catalog install, `/v1/models`, cold/warm non-streaming chat metrics, and streaming refusal; see `docs/benchmarks/2026-04-29-smollm2-api-acceptance.md`.
- The optional MiniLM SafeTensors embedding demo has opt-in API acceptance evidence for catalog install, `/api/embedding-models`, `/v1/models` exclusion, float `/v1/embeddings`, `encoding_format: "base64"` refused with `invalid_request`, and chat refusal; see `docs/benchmarks/2026-04-29-minilm-embeddings-api-acceptance.md`.

## What this evidence does not prove

- It does not prove broad model download success, generation quality, embedding quality, latency, throughput, production readiness, legal/license suitability, or security hardening for exposed deployments.
- It does not prove full OpenAI API parity, streaming chat, real external provider proxying, GGUF runtime/tokenizer execution/generation, ONNX chat/general execution, PyTorch `.bin` loading, or arbitrary SafeTensors/Hugging Face execution.
- Optional networked backend acceptance smoke and large-model runtime artifacts remain separate and should be reviewed before sharing artifacts publicly.
